---
id: "4f0263fa-8da5-4a83-811c-01da1c4acdf7"
level: "task"
title: "Implement cross-PRD duplicate detection with merge-into-older-file resolution"
status: "completed"
priority: "high"
tags:
  - "rex"
  - "smart-add"
  - "dedup"
  - "reshape"
  - "prd-storage"
source: "smart-add"
startedAt: "2026-04-22T18:34:12.341Z"
completedAt: "2026-05-11T21:37:36.037Z"
endedAt: "2026-05-11T21:37:36.037Z"
resolutionType: "code-change"
resolutionDetail: "Implemented cross-PRD duplicate detection for reshape command with newer-item survivor semantics and comprehensive test coverage"
acceptanceCriteria:
  - "Duplicate detection scans items across all prd_*.json files, not just the current branch's file"
  - "When a duplicate is detected, the older PRD file is identified by comparing creation dates from filenames"
  - "Merge writes the updated item to the older PRD file, not the current branch's file"
  - "Merge preserves existing acceptance criteria, tags, and metadata from the older item while incorporating new content"
  - "Non-duplicate proposals are added to the current branch's PRD file as normal"
  - "Two siblings with the same normalized title under the same parent merge into one item; the surviving item keeps the newer item's ID, title, status, priority, and updatedAt"
  - "Non-empty fields present only on the older item (description, acceptanceCriteria, tags, loe, loeRationale, loeConfidence) are copied onto the survivor when the newer item's value is missing or empty"
  - "Two items with the same title but different parents are NOT merged — the reshape pass leaves both intact"
  - "Children of the older item are re-parented under the survivor with no loss of subtree data"
  - "Unit tests cover: newer-wins on conflicting fields, older-fills-missing fields, cross-parent no-merge, child re-parenting, and idempotent re-run (second reshape produces no further changes)"
description: "Add a merge pass to the reshape command that groups items by their parent and detects duplicates among siblings only (by normalized title and structural similarity), then merges each duplicate group into a single item. The newer item (most recent updatedAt/createdAt) is the merge target; for each field, take the newer item's value when present, otherwise fall back to the older item's value. Children of merged items must be re-parented under the surviving item, and the cross-parent case must be a no-op even when titles match. Reuse the existing merge_items folder-tree write path so persistence flows through the canonical .rex/prd_tree/ surface."
---
