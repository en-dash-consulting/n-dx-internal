---
id: "1e652885-f8f6-46b1-8fdc-f7ab1cc9b050"
level: "task"
title: "Add structured PRD status transition trailer to commit messages"
status: "completed"
priority: "high"
tags:
  - "hench"
  - "commit"
  - "prd"
  - "pr-markdown"
  - "reliability"
source: "smart-add"
startedAt: "2026-04-29T18:55:13.022Z"
completedAt: "2026-05-15T13:33:04.769Z"
endedAt: "2026-05-15T13:33:04.769Z"
resolutionType: "code-change"
resolutionDetail: "Implemented all acceptance criteria: (1) N-DX-Status trailer already in shared.ts from prior work; (2) added git interpret-trailers + %(trailers) round-trip tests; (3) created pr-status-trailers.ts extractor + integration tests against fixture branches; (4) trailer already omitted when no status change; (5-9) created commit-msg-watcher.ts with fs.watch() + one-shot setTimeout timer (arms once, cancelled before finalizeRun, timeoutMs=0 disables it), wired into cli-loop.ts, integration tests verify auto-commit at configured timeout, cancellation before expiry, early-write detection, and disabled-timer behavior; (8) added commitMsgTimeoutMs field to HenchConfig (schema/v1.ts + validate.ts) with 300000ms default."
acceptanceCriteria:
  - "Each hench commit that closes a task includes an `N-DX-Status:` trailer with the PRD item id and the from/to status"
  - "Trailer format is parseable by `git interpret-trailers --parse` and round-trips through `git log --format='%(trailers)'`"
  - "PR markdown generator surfaces the closed PRD items by reading `N-DX-Status:` trailers from the branch's commit range — covered by an integration test against a fixture branch"
  - "Trailer is omitted when the run produces no status change (e.g. partial commits, exploratory edits)"
  - "Timer arms exactly once per run when the commit message file is first detected with non-empty initial content"
  - "Timer expiry with a non-empty commit message file commits the staged changes using that message and deletes the file"
  - "Timer is cancelled cleanly if the agent commits or the run terminates before expiry"
  - "Timer duration is configurable via hench config with a 5-minute default"
  - "Integration test simulates an idle agent and verifies the auto-commit fires at the configured timeout"
description: "When the commit message file is first written during a hench run, start a 5-minute timer. On expiry, if the file has non-empty contents, finalize the commit using the current file contents and then remove the file. The timer must be cancelled if the agent commits or removes the file on its own before expiry, and must not fire after the run has otherwise terminated."
---
