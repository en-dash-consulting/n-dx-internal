---
id: "e8f4411d-c876-4f40-8817-3598472d04fd"
level: "task"
title: "Fix hench parent auto-completion cascade silently skipped + add reconciliation sweep (#293)"
status: "pending"
priority: "high"
acceptanceCriteria: []
description: "Fix hench parent auto-completion cascade being silently skipped (GitHub #293): during `hench run --auto --loop` (CLI provider), child tasks are persisted as `completed` on disk but the parent auto-completion cascade never runs — parent features stay `pending` with all children `completed`, and there is no `status_updated` log and no retry/reconciliation, so the tree stays wrong until a human runs `rex update <parent> --status=completed` by hand.\n\n## Context — architecture\n\nTwo tiers are involved. The cascade is triggered in hench's `toolRexUpdateStatus` (`packages/hench/src/tools/rex.ts`), which calls the rex-domain pure function `findAutoCompletions` (`packages/rex/src/core/parent-completion.ts`) re-exported via hench's `src/prd/rex-gateway.ts`. Both `toolRexUpdateStatus` call sites — `updateCompletedTaskStatus` and the finalize path, both in `packages/hench/src/agent/lifecycle/shared.ts` — wrap the whole call in a best-effort `try/catch` that only prints a console warning. PRD state lives in the folder tree (`.rex/prd_tree/`). This task touches BOTH `packages/rex` (add a reconciliation sweep) and `packages/hench` (harden the cascade + surface errors) — no gateway signature changes beyond re-exporting one new rex function through `rex-gateway.ts`.\n\n## Root cause\n\nIn `toolRexUpdateStatus` (`rex.ts`, ~lines 111-170) the sequence is: (1) `store.updateItem(taskId, {status})` persists the child completion; (2) `store.appendLog({event:\"status_updated\"})`; (3) `store.loadDocument()` → `findAutoCompletions(doc.items, taskId)` → per-parent `updateItem` + `appendLog(\"auto_completed\")`. Because the caller's best-effort `try/catch` wraps ALL of this, any throw AFTER step 1 (e.g. an `appendLog` failure at step 2) persists the child's `completed` status while silently dropping the `status_updated` log AND the entire cascade. This exactly matches the observed on-disk signature (child flipped to completed, no `status_updated` event, parent stuck pending). The cascade is fire-once and event-driven — `findAutoCompletions` only walks the ancestor chain of the ONE item passed in — so a missed cascade is never retried.\n\n## Fix direction\n\n1. **Rex core — reconciliation sweep (root fix).** Add a pure function to `packages/rex/src/core/parent-completion.ts`, e.g. `reconcileAutoCompletions(items: PRDItem[]): AutoCompletionResult`, that scans the WHOLE tree bottom-up and returns every parent that is `pending`/`in_progress` but whose children are all terminal (`completed`/`deferred`) — reusing the existing `allChildrenTerminal` / `AUTO_COMPLETABLE_STATUSES` / `TERMINAL_CHILD_STATUSES` logic. Unlike `findAutoCompletions` it is not tied to a single trigger item, so it self-heals parents whose triggering cascade was previously lost. Re-export it through hench's `src/prd/rex-gateway.ts`.\n\n2. **Hench — harden `toolRexUpdateStatus` (`rex.ts`).** (a) Make the three effects independently best-effort: wrap `updateItem`, the `status_updated` `appendLog`, and the cascade in SEPARATE try/catch blocks so a log-append failure cannot cancel the cascade. (b) Replace the single-item `findAutoCompletions(doc.items, taskId)` in the cascade with the whole-tree `reconcileAutoCompletions(doc.items)` so every completion re-sweeps the tree and catches any previously-missed parents — not just the current item's chain. Keep emitting `auto_completed` log entries and the returned summary.\n\n3. **Hench — surface the swallowed error.** Where `updateCompletedTaskStatus` and the finalize path in `shared.ts` catch cascade failures with a console-only warning, also record the failure in the run record diagnostics (so a silent drop is visible after the fact), consistent with how other diagnostics are attached to the run.\n\n## Acceptance criteria\n\n1. A whole-tree reconciliation function exists in rex core, is unit-tested, and returns all eligible (all-children-terminal, still-open) parents bottom-up — including a parent that was missed by a prior event-driven cascade.\n2. In `toolRexUpdateStatus`, a failure of the `status_updated` `appendLog` no longer prevents the parent cascade from running (the child completion AND the parent cascade both persist).\n3. Completing the last child of a feature marks the feature (and any newly-eligible grandparent/epic) completed, with `auto_completed` log entries — and a feature left stuck pending from an earlier missed cascade is healed on the next relevant completion.\n4. A cascade failure is recorded in run diagnostics, not only the console.\n5. Regression tests cover the reconciliation sweep (rex unit) and the independent-best-effort behavior (hench unit/integration).\n\n## Testing guidance (TDD, red→green)\n\n- Rex unit: add tests in `packages/rex/tests/unit/` for `reconcileAutoCompletions` — a tree with a feature whose children are all completed but the feature is still `pending` returns that feature; nested epic propagation; mixed completed/deferred children are terminal; a feature with a `pending` child is NOT returned.\n- Hench unit/integration: build a store (or a mock/stub) where `appendLog` throws once, call `toolRexUpdateStatus(...,'completed')`, and assert the parent still cascaded to completed. Follow existing hench test patterns for store construction.\n- Windows note: normalize CRLF in any file-content comparisons (see the `.replace(/\\r\n/g, \"\n\")` pattern in `tests/integration/rollback.test.ts`).\n\n## Constraints\n\n- TDD: write failing tests first, then implement.\n- Respect the tier boundary: the reconciliation LOGIC belongs in `packages/rex/src/core/parent-completion.ts` (rex domain); hench consumes it only through `src/prd/rex-gateway.ts` (add the re-export there, not a direct deep import). Do not import rex internals directly from hench.\n- Do NOT regress the just-landed #303 (rollback) or #302 (completion-metadata commit) work in `shared.ts`.\n- Keep the existing `findAutoCompletions` function and its callers working; the sweep is additive.\n- `pnpm --filter rex typecheck && pnpm --filter @n-dx/hench typecheck` and the rex + hench unit suites must stay green (modulo the ~38 pre-existing Windows CRLF/path failures tracked in #301)."
---
