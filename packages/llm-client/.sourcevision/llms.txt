# llm-client

> Codebase analysis generated by Sourcevision.
> When using this document: group related items together and separate unrelated concerns.

## Project Overview

- **Files**: 45
- **Lines of code**: 9,980
- **Languages**: TypeScript (42), JSON (3)
- **Zones**: 4 (4 with descriptions)
- **Git**: feature/integrate-codex @ 3acc839
- **Analyzed**: 2026-03-08T16:57:02.832Z

## Architecture Zones

### Root (`root`)

4 files, primarily JSON, TypeScript

- Files: 4
- Cohesion: 0.00, Coupling: 0.00
- Detail: `.sourcevision/zones/root/context.md`

### Src (`src`)

13 files, primarily TypeScript

- Files: 13
- Cohesion: 0.77, Coupling: 0.23
- Entry points: src/api-provider.ts, src/cli-provider.ts, src/codex-cli-provider.ts, src/create-client.ts, src/llm-client.ts (+7 more)
- Detail: `.sourcevision/zones/src/context.md`

### Src 2 (`src-2`)

19 files, primarily TypeScript

- Files: 19
- Cohesion: 0.56, Coupling: 0.44
- Entry points: src/config.ts
- Detail: `.sourcevision/zones/src-2/context.md`

### Unit (`unit`)

9 files, primarily TypeScript

- Files: 9
- Cohesion: 1.00, Coupling: 0.00
- Detail: `.sourcevision/zones/unit/context.md`

## Key Dependencies

### Most-Imported Files

- `src/types.ts` (11 imports)
- `src/config.ts` (6 imports)
- `src/create-client.ts` (4 imports)
- `src/provider-registry.ts` (4 imports)
- `src/token-usage.ts` (4 imports)
- `src/api-provider.ts` (3 imports)
- `src/cli-provider.ts` (3 imports)
- `src/codex-cli-provider.ts` (3 imports)
- `src/exec.ts` (3 imports)
- `src/auth.ts` (2 imports)

### External Packages

- `vitest` (used by 20 files)
- `@anthropic-ai/sdk` (used by 3 files)

## Findings

### Anti-patterns

- A shared base types zone is missing: both llm-provider-core and llm-cli-utilities need the same fundamental types (provider config shapes, token structs, project dir records), causing core to import upward into utilities to get them. Introducing a zero-dependency types-only zone that both source zones import downward from would eliminate the cycle with no behavioral change. [critical]
- HTTP and subprocess provider strategies (api-provider vs cli-provider/codex-cli-provider) are co-located in the same zone with no sub-boundary — a consumer cannot depend on just the HTTP strategy without also pulling in subprocess execution code, and adding a new transport requires editing files adjacent to unrelated transports. [warning]
- Findings src/0 (12 entry points in src zone) and global/2 (all zones follow single public entry point pattern) are mutually exclusive at the intra-package import level. If src has 12 files imported from src-2, the public.ts single-surface convention is being bypassed for 11 of them. The automated heuristic (src/0) is based on call-graph edge counting and is more precise than the LLM's general observation (global/2). Until the 12 entry points are audited, global/2 should not be relied upon as a confirmed invariant — it may describe intent rather than reality. [warning]

### Patterns

- The asymmetry between test coverage (core only) and source coupling (core ↔ utilities) means the package's test suite validates the intended architecture but not the actual architecture — a dedicated utility test zone would close this gap. [info]
- The three source zones form a layered architecture (utilities → core ← tests) that is one refactor away from being a clean strict DAG — only 3 import edges need to be redirected or extracted into a shared types module to achieve full unidirectionality. [info]
- At least 13 findings (global/0, 4, 6, 8, 11, 17, 20, 22, 25, 31, 32, 33, 34) independently describe aspects of the same 3 reverse import edges from different angles: automated heuristic edge counting, two independent LLM analysis passes, and multiple meta-evaluation passes all converged unprompted on the same root cause and the same fix candidate. This cross-method convergence — without any single pass being able to see the others — is strong evidence the finding is real and not a false positive, and justifies treating it as the highest-priority work item even before clean re-analysis confirms edge persistence. [info]
- Canonical action plan distilled from findings global/19, 28, 29, 30, and 31 with correct sequencing: Step 0 (gate) — re-run sourcevision analyze; if the 3 reverse edges no longer exist after clean re-analysis, steps 1–3 are unnecessary. Step 1 — if edges persist, extract exactly four files (types.ts, llm-types.ts, provider-interface.ts, project-dirs.ts — all already classified as 'types' archetype) into a new zero-dependency zone; update import paths in api-provider.ts, cli-provider.ts, and auth.ts only. Step 2 — add utility test coverage for every export in the src-2 zone. Step 3 — add one integration test asserting a constructed session contains a valid auth token. Steps 1–3 are ordered; step 0 is the prerequisite gate for all three. [critical]
- Finding global/6 describes a near-clean layered architecture that is 'one refactor away' from a strict DAG. The underlying problem (3 reverse edges) is already captured at appropriate severity in findings 4, 8, 11, and 12. Finding 6 is corroborating evidence, not an independent critical defect — retaining critical severity double-counts the same root cause. [warning]
- Finding global/7 is a positive architectural property (no zone imports from package-root at runtime) and must carry 'info' severity per evaluation constraints. Its critical designation conflates a confirmed-good invariant with genuine structural problems in findings 8 and 11, distorting the severity distribution. [warning]
- Finding inflation: 53 findings across 6 zones encode 4 action items. The ratio of corroborating evidence to independent findings is approximately 12:1. For future analysis passes on packages below 50 files, suppressions should be applied for: (a) findings that describe the same root cause as an already-critical finding, (b) findings that reference positive invariants with no prescribed action, and (c) meta-findings that describe severity miscategorizations rather than structural defects. Applying these suppressions here reduces the working set to 6 actionable findings. [info]
- Findings 0, 4, 6, 8, and 11 converge on one root cause: no zero-dependency base types zone exists, so llm-provider-core imports upward into llm-cli-utilities to access shared config and token shapes. This single missing layer is the origin of all three compounding risks in finding 11. It should be tracked as one work unit, not five separate issues. [critical]
- Findings 3 and 9 are complementary and do not contradict each other. Finding 3 (four zones is appropriate granularity) operates at the inter-zone level; finding 9 (HTTP and subprocess strategies are co-located without sub-boundary) operates at the intra-zone level. Both are correct simultaneously. Finding 3 does not imply that all intra-zone organization is optimal. [info]
- Findings 7 (critical, positive boundary) and 2 (info, positive entry-point pattern) share the same underlying characteristic — both describe architectural properties that are working correctly and should be preserved. Neither warrants any action. They should be treated as invariants to protect during the types-extraction refactor described in finding 14, not as issues to address. [info]
- ... and 10 more

### Relationships

- No zone imports from package-root at runtime — configuration is consumed by external tooling only. This clean boundary means config changes cannot introduce runtime regressions in source zones. [critical]

### Observations

- Bidirectional coupling: "src" ↔ "src-2" (3+23 crossings) — consider extracting shared interface [warning]
- All zones use a single public entry point pattern consistent with the monorepo's src/public.ts convention, making the package boundary explicit and auditable. [info]
- Four zones for a 41-file package indicates appropriate granularity — the community detection has produced meaningful boundaries without over-fragmenting a small package. [info]
- The bidirectional dependency between src and src-2 (25 down, 3 up) is the primary architectural risk — eliminating the 3 upward imports would make the layering clean and unidirectional. [warning]
- Severity reassessment summary: global/6 downgraded critical→warning (double-counts root cause already in global/8); global/7 downgraded critical→info (positive architectural property, must be info per evaluation constraints); global/11 upgraded info→critical (cluster-criticality justification confirmed across global/22 and 23 — three co-occurring sub-threshold risks on the same change surface eliminates all defensive layers); global/14 downgraded critical→info (meta-finding about global/7 which is itself a positive property); global/15 downgraded critical→warning (descriptive convergence finding, root cause independently covered by global/8 at critical); global/31 downgraded critical→warning (planning prerequisite, not a structural defect); src-2/1 and src-2/6 downgraded warning→info (missing zone name is a process gap, not an architectural defect or risk). [info]
- The stale zones manifest (status: 'running') is an independent corroboration of why the src-2 zone received no name from automated enrichment (src-2/1): the enrichment LLM call likely never completed or its output was not persisted. This means the zone naming failure is a process artifact, not a signal about zone coherence. Finding global/19's inference that 'enrichment failure corroborates fuzzy zone boundary' is therefore invalid — the failure source is a crashed process, not ambiguous content. [info]
- Isolated files — no import edges between 4 files, cohesion is unmeasurable (reported as 0) [info] (root)
- 12 entry points — wide API surface, consider consolidating exports [warning] (src)
- Findings src/0 (12 entry points, automated heuristic) and global/2 (single public entry point pattern, LLM pass 1) contradict each other. The automated heuristic counts files in the src zone that have incoming import edges from other zones; if 12 such files exist, the src/public.ts single-entry-point convention is not being honored at the zone boundary level. Audit which of the 12 src zone files are imported directly by src-2 zone files and determine whether those imports should be consolidated to route through a single gateway or public.ts re-export. [warning] (src)
- Contains 42% of project files (19/45) — may be too broad, consider splitting [warning] (src-2)
- ... and 5 more

### Suggestions

- tests/unit/ contains files from 3 zones (src, src-2, unit) — consider grouping into subdirectories to clarify architectural boundaries [info]
- Document the one-to-one test-to-module mapping convention used in llm-provider-tests in a PACKAGE_GUIDELINES.md or test/README.md entry — this implicit convention is enforced only by social norms and will silently erode as the package grows. Codifying it prevents new contributors from breaking the mapping during refactors. [warning]
- The 3 reverse imports (llm-provider-core → llm-cli-utilities) combined with zero utility test coverage and implicit session↔auth coupling form a compounding risk cluster: a single change to auth.ts or a utility config shape can silently break provider construction with no test signal at any layer. Treat these three issues as a single fix unit: extract shared types, add a utility test zone, and add a session-auth integration test simultaneously. [info]
- The compounding risk cluster (finding 11, now critical) should be resolved as a single sequential work unit: step 1 — introduce a new zero-dependency zone containing only shared provider config shapes and token structs, update llm-provider-core and llm-cli-utilities to import downward from it, eliminating the 3 reverse edges; step 2 — add a test zone for llm-cli-utilities with at minimum one test per exported utility to close the coverage gap identified in finding 5; step 3 — add one integration-level test asserting that a constructed session contains a valid auth token, making the session–auth coupling in finding 11 regression-protected. Steps must be done in this order: the types extraction in step 1 defines the stable interface that steps 2 and 3 test against. [critical]
- The concrete prerequisite step for finding 17 is: inspect the import lines in api-provider.ts, cli-provider.ts, and auth.ts and list every symbol imported from files in the src-2 zone. Cross-reference with types.ts, llm-types.ts, provider-interface.ts, and project-dirs.ts — all classified as 'types' archetype — to identify which shared type definitions are currently anchored in the utilities zone. These are the symbols that must move to the new zero-dependency zone to eliminate the 3 reverse edges. files.ts and exec.ts contain logic and should not move; only pure type/interface/constant definitions qualify. [warning]
- The exact symbols forcing the 3 reverse import edges from llm-provider-core into llm-cli-utilities are: ClaudeConfig, AuthMode, TokenUsage (from types.ts), LLMConfig, CodexConfig (from llm-types.ts), and the provider interface from provider-interface.ts — all already classified as 'types' archetype per global/15 and global/18. The new shared zone must contain exactly these files and no others. Logic-containing files (exec.ts, files.ts, config.ts resolver functions, token-usage.ts parsing helpers) must not move; only pure type/interface/constant definitions qualify. [warning]
- The four ordered action items synthesized across this finding set should be tracked as a single PRD epic with four sequential tasks rather than as independent findings: Task 1 (gate) — re-run sourcevision analyze and confirm whether the 3 reverse edges persist in a clean zone map. Task 2 — if edges persist, attempt zone pinning of types.ts, llm-types.ts, provider-interface.ts, project-dirs.ts before attempting an import-path refactor; only proceed to refactor if pinning is unavailable or produces an unstable boundary. Task 3 — add utility test coverage for all exports in the current src-2 zone. Task 4 — add one session-auth integration test asserting a constructed session contains a valid auth token. Tasks 3 and 4 are independent of Tasks 1–2 but should be written against the stable interface produced by Task 2 if it runs. [warning]
- The minimum-effort resolution for the 3 reverse import edges is zone pinning via sourcevision configuration, not a structural refactor. Add a zone configuration entry pinning types.ts, llm-types.ts, provider-interface.ts, and project-dirs.ts as a named anchor cluster. This instructs Louvain to place these files in a fixed foundation zone on every re-analysis, drawing zone boundaries so provider service files (api-provider.ts, cli-provider.ts, auth.ts) cluster above them — eliminating reverse edges with zero import-path changes and zero test disruption. Attempt zone pinning first after re-running sourcevision analyze. Only proceed to the import-path refactor described in global/31 if pinning produces an unstable boundary on re-analysis. [warning]
- The sequential work unit in finding 14 has an implicit prerequisite: before introducing the new shared types zone, identify the exact set of types currently forcing the 3 reverse imports from llm-provider-core into llm-cli-utilities. These are the provider config shapes and token structs named in finding 8. Cataloguing them first ensures the new zone contains exactly what eliminates the cycle and nothing more, preventing the new zone from becoming a general-purpose dumping ground that recreates the problem at one layer down. [critical]
- global/22 is marked critical and states 'Findings 0, 4, 6, 8, and 11 converge on one root cause: no zero-dependency base types zone exists'. This is a convergence observation — it adds epistemic weight to findings already captured at appropriate severity (global/9 is critical, global/35 is critical). Convergence evidence is not itself a new critical defect. Retaining critical severity here inflates the must-fix count and makes global/22 indistinguishable from the actual action items. Severity should be 'warning'. [warning]
- ... and 10 more

## Next Steps

Prioritized work items derived from analysis:

### High Priority

1. **The compounding risk cluster (finding 11, now critical) sho… (+5 related)**

### Medium Priority

1. **Add a short ZONE.md at the src-2 zone root documenting: (1) the domain name cho…**
   Files: `src/auth.ts`, `src/config.ts`, `src/exec.ts`

1. **Manually assign a domain-specific name to this zone (e.g. 'llm-cli-utilities') …**
   Files: `src/auth.ts`, `src/config.ts`, `src/exec.ts`

1. **Run `sourcevision analyze` to produce a clean zone manifest before acting on an…**
   Files: `src/auth.ts`, `src/config.ts`, `src/exec.ts`

1. **Contains 42% of project files (19/45) — may be too broad, consider splitting**
   Files: `src/auth.ts`, `src/config.ts`, `src/exec.ts`

1. **Generic zone name "Src 2" — enrichment did not assign a meaningful name reflect…**
   Files: `src/auth.ts`, `src/config.ts`, `src/exec.ts`

1. **The sourcevision manifest.json records the zones module as status 'running' wit…**
   Files: `src/auth.ts`, `src/config.ts`, `src/exec.ts`

1. **Audit the 12 files in the src zone that have incoming import edges from src-2. …**
   Files: `src/api-provider.ts`, `src/cli-provider.ts`, `src/codex-cli-provider.ts`

1. **12 entry points — wide API surface, consider consolidating exports**
   Files: `src/api-provider.ts`, `src/cli-provider.ts`, `src/codex-cli-provider.ts`

1. **Findings src/0 (12 entry points, automated heuristic) and global/2 (single publ…**
   Files: `src/api-provider.ts`, `src/cli-provider.ts`, `src/codex-cli-provider.ts`

1. **Finding global/6 describes a near-clean layered architectur… (+5 related)**

### Low Priority

1. **Zone "Root" (root) has at-risk risk (score: 0.50, cohesion: 0.00, coupling: 0.0…**
   Files: `package-lock.json`, `package.json`, `tsconfig.json`

1. **After clean re-analysis, if the src-2 zone boundary is confirmed, assign the zo…**
   Files: `src/auth.ts`, `src/config.ts`, `src/exec.ts`

1. **If a clean re-analysis confirms the src-2 boundary, assign the zone name 'llm-p…**
   Files: `src/auth.ts`, `src/config.ts`, `src/exec.ts`

1. **Before treating the 12 entry-point count as a public API violation, confirm whe…**
   Files: `src/api-provider.ts`, `src/cli-provider.ts`, `src/codex-cli-provider.ts`

1. **tests/unit/ contains files from 3 zones (src, src-2, unit) — consider grouping …**

1. **The 3 reverse imports (llm-provider-core → llm-cli-utilities) combined with zer…**

1. **global/23 infers that the src-2 zone enrichment failure 'independently corrobor…**

## File Inventory

| Path | Role | Archetype | Zone |
|------|------|-----------|------|
| `package-lock.json` | generated |  | root |
| `package.json` | config |  | root |
| `src/api-provider.ts` | source | service | src |
| `src/auth.ts` | source | utility | src-2 |
| `src/cli-provider.ts` | source | service | src |
| `src/codex-cli-provider.ts` | source | service | src |
| `src/config.ts` | source | config | src-2 |
| `src/create-client.ts` | source | service | src |
| `src/exec.ts` | source | utility | src-2 |
| `src/help-format.ts` | source | utility | src-2 |
| `src/json.ts` | source | utility | src-2 |
| `src/llm-client.ts` | source | service | src |
| `src/llm-config.ts` | source | config | src |
| `src/llm-types.ts` | source | types | src |
| `src/output.ts` | source | utility | src-2 |
| `src/project-config.ts` | source | utility | src-2 |
| `src/project-dirs.ts` | source | types | src-2 |
| `src/provider-interface.ts` | source | types | src |
| `src/provider-registry.ts` | source | store | src |
| `src/provider-session.ts` | source | service | src |
| `src/public.ts` | source | entrypoint | src-2 |
| `src/suggest.ts` | source | utility | src-2 |
| `src/token-usage.ts` | source | utility | src |
| `src/types.ts` | source | types | src |
| `tests/unit/api-provider.test.ts` | test |  | unit |
| `tests/unit/auth.test.ts` | test |  | src-2 |
| `tests/unit/cli-provider.test.ts` | test |  | unit |
| `tests/unit/config.test.ts` | test |  | src-2 |
| `tests/unit/create-client.test.ts` | test |  | unit |
| `tests/unit/exec.test.ts` | test |  | src-2 |
| `tests/unit/help-format.test.ts` | test |  | src-2 |
| `tests/unit/json.test.ts` | test |  | src-2 |
| `tests/unit/llm-client.test.ts` | test |  | unit |
| `tests/unit/llm-config.test.ts` | test |  | src |
| `tests/unit/output.test.ts` | test |  | src-2 |
| `tests/unit/project-config.test.ts` | test |  | src-2 |
| `tests/unit/provider-interface.test.ts` | test |  | unit |
| `tests/unit/provider-registry.test.ts` | test |  | unit |
| `tests/unit/provider-session.test.ts` | test |  | unit |
| `tests/unit/public-api.test.ts` | test |  | src-2 |
| `tests/unit/suggest.test.ts` | test |  | src-2 |
| `tests/unit/token-usage.test.ts` | test |  | unit |
| `tests/unit/types.test.ts` | test |  | unit |
| `tsconfig.json` | config |  | root |
| `vitest.config.ts` | config |  | root |

## Tooling

This project uses n-dx for codebase analysis (Sourcevision) and requirement
tracking (Rex). MCP tools are available for interactive access to analysis
data and PRD management. Skills available: /plan, /status, /capture,
/zone, /work, /configure.
