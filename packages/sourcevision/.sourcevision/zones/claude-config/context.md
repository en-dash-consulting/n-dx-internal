# Claude Config — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Claude Config (`claude-config`)
Files: 2, Cohesion: 0.00, Coupling: 0.00
Risk: at-risk (score: 0.50)
Description: Stores Claude Code editor settings and local overrides for the workspace.
Lines: 19

</zone>

<files>

.claude/settings.json (JSON, 5 lines, other)
.claude/settings.local.json (JSON, 14 lines, other)

</files>

<findings>

[observation] [info] Isolated files — no import edges between 2 files, cohesion is unmeasurable (reported as 0)
[observation] [info] Zone contains only editor configuration — zero cohesion and coupling scores are expected and correct for this role.
[observation] [warning] settings.local.json may contain developer-specific overrides that shouldn't be committed; verify .gitignore excludes it.
[suggestion] [info] Zone "Claude Config" (claude-config) has at-risk risk (score: 0.50, cohesion: 0.00, coupling: 0.00) — approaching architectural risk thresholds

</findings>

<insights>

- Isolated files — no import edges between 2 files, cohesion is unmeasurable (reported as 0)
- These files are editor-specific and should be reviewed for accidental secret exposure in settings.local.json
- No entry points or imports mean this zone is fully isolated — correct for config-only files
- Consider adding settings.local.json to .gitignore if it contains developer-specific overrides
- Zone contains only editor configuration — zero cohesion and coupling scores are expected and correct for this role.
- settings.local.json may contain developer-specific overrides that shouldn't be committed; verify .gitignore excludes it.

</insights>
