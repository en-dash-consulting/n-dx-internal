/**
 * Output utility re-exports for the hench package.
 */

// Re-export output utilities for convenient access
export {
  setQuiet,
  isQuiet,
  info,
  result,
  section,
  subsection,
  stream,
  detail,
} from "./output.js";
