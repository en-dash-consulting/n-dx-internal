# PRD Fix Command — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: PRD Fix Command (`prd-fix-command`)
Files: 4, Cohesion: 0.25, Coupling: 0.75
Risk: catastrophic (score: 0.75)
Description: Encapsulates the fix command that detects and repairs structural inconsistencies in the PRD tree.
Entry points: src/cli/commands/fix.ts
Lines: 1230

</zone>

<files>

src/cli/commands/fix.ts (TypeScript, 139 lines, source)
src/core/fix.ts (TypeScript, 258 lines, source)
tests/unit/cli/commands/fix.test.ts (TypeScript, 420 lines, test)
tests/unit/core/fix.test.ts (TypeScript, 413 lines, test)

</files>

<imports>

Internal:
  src/cli/commands/fix.ts → src/core/fix.ts {detectIssues, applyFixes}
  src/cli/commands/fix.ts → src/core/fix.ts {FixAction, FixKind}
  tests/unit/cli/commands/fix.test.ts → src/cli/commands/fix.ts {cmdFix}
  tests/unit/core/fix.test.ts → src/core/fix.ts {detectTimestampIssues, detectOrphanBlockedBy, detectParentChildMisalignment, detectIssues, applyFixes}

Outgoing (this zone → other zones):
  → analyze-engine: src/core/fix.ts → src/schema/index.ts; tests/unit/cli/commands/fix.test.ts → src/schema/index.ts; tests/unit/core/fix.test.ts → src/schema/index.ts
  → cli-command-layer: src/cli/commands/fix.ts → src/cli/output.ts
  → mcp-cli-bootstrap: src/cli/commands/fix.ts → src/cli/commands/constants.ts
  → prd-graph-utilities: src/core/fix.ts → src/core/tree.ts
  → remote-sync-adapters: src/cli/commands/fix.ts → src/store/index.ts

Incoming (other zones → this zone):
  ← cli-command-layer: src/cli/index.ts → src/cli/commands/fix.ts

</imports>

<findings>

[observation] [warning] High coupling (0.75) — 3 imports target "analyze-engine"
[observation] [warning] Low cohesion (0.25) — files are loosely related, consider splitting this zone
[observation] [info] At only 4 files this zone is cohesion-constrained by size — the low score reflects graph sparsity rather than poor design; the fix/core split follows the established command-layer pattern correctly.
[observation] [info] Both unit tests (fix.test.ts for core and CLI) are present and co-located — good coverage discipline for a repair command where subtle regressions are high-risk.
[observation] [info] Coupling of 0.75 traces almost entirely to unit-analyze imports; if the fix logic grows, extracting a src/core/fix-strategies.ts would improve testability without increasing cross-zone coupling.
[relationship] [info] The fix zone maintains a near-unidirectional import relationship with unit-analyze (outgoing only, 1 return at most), making it one of the few zones that correctly treats the analyze engine as a stable upstream dependency.
[suggestion] [info] The fix zone is confirmed live (command documented in usage(), full CLI support verified). Remove any dead-code uncertainty from zone documentation and treat it as an active leaf zone alongside unit-store and e2e.
[suggestion] [critical] Zone "PRD Fix Command" (prd-fix-command) has catastrophic risk (score: 0.75, cohesion: 0.25, coupling: 0.75) — requires immediate architectural intervention

</findings>

<insights>

- Low cohesion (0.25) — files are loosely related, consider splitting this zone
- High coupling (0.75) — 3 imports target "analyze-engine"
- The zone is tightly scoped to a single command with matching production and test files — a clean, intentional grouping.
- Low cohesion (0.25) reflects that the zone is small and leans entirely on the Analyze Engine for domain logic, with little internal connectivity.
- High coupling (0.75) is expected given the fix operation must traverse the full PRD graph; no architectural concern here.
- At only 4 files this zone is cohesion-constrained by size — the low score reflects graph sparsity rather than poor design; the fix/core split follows the established command-layer pattern correctly.
- Coupling of 0.75 traces almost entirely to unit-analyze imports; if the fix logic grows, extracting a src/core/fix-strategies.ts would improve testability without increasing cross-zone coupling.
- Both unit tests (fix.test.ts for core and CLI) are present and co-located — good coverage discipline for a repair command where subtle regressions are high-risk.
- With only 1 return import from unit-analyze (versus 9+ outgoing), the fix zone is one of the cleanest consumers of the analyze engine — the unidirectional boundary here is a reference pattern for other zones to emulate
- The fix zone maintains a near-unidirectional import relationship with unit-analyze (outgoing only, 1 return at most), making it one of the few zones that correctly treats the analyze engine as a stable upstream dependency.
- The fix zone is confirmed live code: the fix command is documented in the CLI help text via usage() in src/cli/commands/constants.ts, has full --dry-run and --format=json flag support, and is reachable through the CLI entry point. The prior uncertainty about live vs dead code is resolved.
- The fix zone is confirmed live (command documented in usage(), full CLI support verified). Remove any dead-code uncertainty from zone documentation and treat it as an active leaf zone alongside unit-store and e2e.
- [call graph] 136 internal calls, 15 outgoing, 1 incoming (cohesion: 0.9, coupling: 0.1)

</insights>
