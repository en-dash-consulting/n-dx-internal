{
  "schema": "rex/v1",
  "title": "rex",
  "items": [
    {
      "id": "ac5a194f-8f27-4f5c-9dd4-7118a77d2dd1",
      "title": "Address observation issues (2 findings)",
      "status": "pending",
      "level": "feature",
      "description": "- Contains 71% of project files (36/51) — may be too broad, consider splitting\n- The store layer uses a read-modify-write pattern in file-adapter.ts without file locking or optimistic concurrency control; concurrent CLI and MCP invocations against the same .rex/ directory could produce data races.",
      "priority": "high",
      "source": "sourcevision"
    },
    {
      "id": "d9f84928-46a5-4bd4-ba9a-5a9f3cb72a21",
      "title": "Address pattern issues (5 findings)",
      "status": "pending",
      "level": "feature",
      "description": "- No circular dependencies exist in the import graph, but the store→core dependency creates a latent risk: any future core→store dependency would immediately form a cycle.\n- CLI commands init.ts and validate.ts perform direct filesystem I/O (readFile/writeFile) instead of going through the store abstraction, creating a second persistence path that would break if the storage backend changes.\n- PRIORITY_ORDER mapping is defined identically in three files (core/canonical.ts, core/next-task.ts, analyze/propose.ts) rather than exported from a single source of truth.\n- Store barrel export (store/index.ts) exposes FileStore class and ensureRexDir utility, leaking filesystem-specific implementation details through the public API and defeating the adapter pattern.\n- Store layer imports tree manipulation functions (findItem, insertChild, updateInTree, removeFromTree) from core/tree.js, creating a dependency inversion where the persistence layer depends on business logic utilities.",
      "priority": "high",
      "source": "sourcevision"
    },
    {
      "id": "73c02fb0-5246-40d5-b87a-c8f42c78b5c5",
      "title": "Address anti-pattern issues (9 findings)",
      "status": "pending",
      "level": "feature",
      "description": "- Every store operation (addItem, updateItem, removeItem) performs a full loadDocument-modify-saveDocument cycle (file-adapter.ts:40-69), meaning concurrent CLI or MCP calls on the same .rex directory will race on the JSON file with no locking, potentially causing silent data loss via last-write-wins.\n- MCP tool handlers duplicate CLI command orchestration logic wholesale (status retrieval, next-task selection, item addition, update-with-log). Neither entry point delegates to a shared service layer, so any business logic change (e.g., adding validation or audit rules) must be applied in two places.\n- PRDItem has an index signature `[key: string]: unknown` (v1.ts:21) which disables TypeScript excess-property checking on every PRDItem value. Combined with `.passthrough()` on the Zod schema, arbitrary properties silently survive both compile-time and runtime validation, defeating the purpose of the typed schema.\n- PRDItemSchema is exported as `z.ZodType<Record<string, unknown>>` (validate.ts:18), erasing the inferred type that Zod would normally provide. Every consumer must cast the validated result `as PRDDocument` or `as PRDItem`, creating a chain of unsafe casts (file-adapter.ts:27,37,50,78) that provide false confidence while bypassing actual type safety.\n- VALID_STATUSES and VALID_PRIORITIES in update.ts:6-12 are hardcoded string Sets that duplicate the enum values defined in schema/validate.ts (ItemStatusSchema, PrioritySchema). The MCP layer uses Zod enums directly (mcp.ts:110,163), so the CLI and MCP validate against different sources of truth.\n- analyze --accept (analyze.ts:125-169) performs sequential store.addItem() calls in a loop, each of which reads and rewrites the entire prd.json file. For N proposals this is O(N) full file rewrites with no atomicity — a crash at item K leaves K items written and the rest lost, with no rollback mechanism.\n- callSourceVisionMCP (recommend.ts:38) spawns a child process with no timeout or AbortSignal. If the sourcevision binary hangs or produces no output, the CLI blocks indefinitely with no way for the user to recover short of killing the process.\n- readLog() (file-adapter.ts:105) uses a bare `as LogEntry` cast on each JSON.parse result without Zod validation, while appendLog() validates before writing. This asymmetric validation at the serialization boundary means hand-edited or corrupted log files can inject malformed data.\n- sortItems (canonical.ts:14) and sortByPriority (next-task.ts:11) are near-duplicate sort functions in core with a subtle behavioral difference: sortItems recurses into children while sortByPriority does not. A single parameterized sort in canonical.ts with an optional `recursive` flag would eliminate the hidden divergence risk.",
      "priority": "critical",
      "source": "sourcevision"
    },
    {
      "id": "d112c918-4338-4c89-97d8-63cbd93106f2",
      "title": "Address suggestion issues (6 findings)",
      "level": "feature",
      "status": "pending",
      "description": "- Log event names are raw inline strings with no shared enum/constant. CLI uses \"item_updated\" (update.ts:68) while MCP uses \"status_changed\" (mcp.ts:129) for the same operation, making log consumers handle divergent event names.\n- MCP add_item handler (mcp.ts:160-195) skips the parent-level hierarchy validation that CLI cmdAdd enforces (add.ts:36-55), allowing invalid level nesting through the MCP entry point.\n- PRIORITY_ORDER duplicated 3x across canonical.ts:7, next-task.ts:4, propose.ts:25 with inconsistent typing (Record<Priority,number> vs Record<string,number>). Extract to a single export in schema or core.\n- REX_DIR is redeclared as a private const in mcp.ts:14 instead of importing from cli/commands/constants.ts where it is already exported. A rename would silently diverge.\n- config.adapter is never read at any call site — all 8 createStore() calls hardcode \"file\". The adapter config field and store factory switch are dead code paths.\n- validate.ts reads config.json and prd.json via raw fs.readFile (lines 23, 46) bypassing the store layer. init.ts writes all files via raw fs.writeFile (lines 27, 42, 52, 62). Both would break with a non-file adapter.",
      "priority": "high",
      "source": "sourcevision"
    }
  ]
}
