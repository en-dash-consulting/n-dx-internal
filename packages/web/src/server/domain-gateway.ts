/**
 * Domain MCP gateway.
 *
 * Keeps runtime MCP factory imports centralized so route handlers stay
 * transport-focused and package boundaries remain explicit.
 */

export { createSourcevisionMcpServer } from "sourcevision";
