# Rex Core Utilities — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Rex Core Utilities (`rex-core-utilities`)
Files: 4, Cohesion: 0.18, Coupling: 0.82
Risk: catastrophic (score: 0.82)
Description: Isolated utility zone providing keyword extraction and criteria verification primitives consumed by the main Rex PRD Engine.
Entry points: packages/rex/src/core/keywords.ts, packages/rex/src/core/verify.ts
Lines: 898

</zone>

<files>

packages/rex/src/core/keywords.ts (TypeScript, 50 lines, source)
packages/rex/src/core/verify.ts (TypeScript, 327 lines, source)
packages/rex/tests/unit/core/keywords.test.ts (TypeScript, 129 lines, test)
packages/rex/tests/unit/core/verify.test.ts (TypeScript, 392 lines, test)

</files>

<imports>

Internal:
  packages/rex/src/core/verify.ts → packages/rex/src/core/keywords.ts {extractKeywords, scoreMatch, STOP_WORDS}
  packages/rex/tests/unit/core/keywords.test.ts → packages/rex/src/core/keywords.ts {extractKeywords, scoreMatch, STOP_WORDS}
  packages/rex/tests/unit/core/verify.test.ts → packages/rex/src/core/verify.ts {extractKeywords, scoreMatch, mapCriteriaToTests, collectVerifiableTasks, findTestFiles, verify}

Outgoing (this zone → other zones):
  → rex-prd-engine: packages/rex/src/core/verify.ts → packages/rex/src/core/tree.ts; packages/rex/src/core/verify.ts → packages/rex/src/schema/index.ts; packages/rex/tests/unit/core/verify.test.ts → packages/rex/src/schema/index.ts

Incoming (other zones → this zone):
  ← rex-prd-engine: packages/rex/src/cli/commands/status-sections.ts → packages/rex/src/core/verify.ts; packages/rex/src/cli/commands/status.ts → packages/rex/src/core/verify.ts; packages/rex/src/cli/commands/status.ts → packages/rex/src/core/verify.ts; packages/rex/src/cli/commands/verify.ts → packages/rex/src/core/verify.ts; packages/rex/src/cli/mcp-tools.ts → packages/rex/src/core/verify.ts; packages/rex/src/core/epic-correlation.ts → packages/rex/src/core/keywords.ts; packages/rex/src/core/next-task.ts → packages/rex/src/core/keywords.ts; packages/rex/src/public.ts → packages/rex/src/core/keywords.ts

</imports>

<findings>

[observation] [warning] High coupling (0.82) — 3 imports target "rex-prd-engine"
[observation] [warning] Low cohesion (0.18) — files are loosely related, consider splitting this zone
[observation] [warning] Bidirectional imports between rex-core-utilities and rex-prd-engine (3 outbound, 8 inbound) suggest verify.ts or keywords.ts depend on types defined in the main engine — this circular shape should be resolved by moving shared types to schema or making the dependency strictly one-directional.
[observation] [warning] Cohesion 0.18 and coupling 0.82 indicate two unrelated utilities grouped together; splitting them into their dominant consumer zones or absorbing them into rex-prd-engine would eliminate the bidirectional dependency.
[observation] [info] With only 2 production files below the 5-file measurement threshold, the extreme coupling score primarily reflects that both files are heavily imported externally rather than indicating design complexity that requires structural intervention.
[pattern] [warning] keywords.ts and verify.ts share no internal imports and exist as a zone solely because they both import from and are imported by rex-prd-engine — absorbing both files directly into rex-prd-engine would eliminate the zone, break the bidirectional cycle, and improve the engine's self-contained cohesion without adding external coupling.
[relationship] [critical] The 3 outbound imports from rex-core-utilities into rex-prd-engine (likely schema/type imports) combined with 8 inbound imports from the engine create a type-dependency loop — verify.ts or keywords.ts depend on types defined in the very zone that consumes them, which is structurally identical to a circular import even if the build currently resolves it.
[anti-pattern] [warning] rex-core-utilities has zero internal imports between its two production files and exists solely because both are co-imported by rex-prd-engine — it is a community-detection artifact, not an architectural boundary. Dissolving it by moving keywords.ts and verify.ts into rex-prd-engine eliminates 11 cross-zone edges and three governance alerts with no behavior change.
[suggestion] [info] keywords.ts and verify.ts have an inbound-dominant import profile (8 in, 3 out from rex-prd-engine) that identifies them as library modules extracted from the engine, not CLI command satellites — the CLI-only content governance policy applied to other rex satellites does not apply here; document the distinction if absorption into rex-prd-engine is deferred to avoid applying the wrong governance rule to these files.
[suggestion] [info] Zone "Rex Core Utilities" (rex-core-utilities) has catastrophic risk (score: 0.82, cohesion: 0.18, coupling: 0.82) — unreliable: zone has only 4 files (minimum 5 for reliable metrics)

</findings>

<insights>

- Low cohesion (0.18) — files are loosely related, consider splitting this zone
- High coupling (0.82) — 3 imports target "rex-prd-engine"
- Cohesion 0.18 is the lowest in this batch — keywords.ts and verify.ts serve different domain concerns and share no internal imports, suggesting they were grouped by graph proximity rather than semantic affinity.
- The bidirectional import relationship with rex-prd-engine (8 inbound from rex-unit, 3 outbound to rex-unit) is a structural smell; verify.ts likely imports schema or tree types from the main zone while also being consumed by it.
- If keywords.ts and verify.ts each have only one consumer zone (rex-prd-engine), both violate the two-consumer rule and are candidates for absorption into the main engine zone.
- Cohesion 0.18 and coupling 0.82 indicate two unrelated utilities grouped together; splitting them into their dominant consumer zones or absorbing them into rex-prd-engine would eliminate the bidirectional dependency.
- Bidirectional imports between rex-core-utilities and rex-prd-engine (3 outbound, 8 inbound) suggest verify.ts or keywords.ts depend on types defined in the main engine — this circular shape should be resolved by moving shared types to schema or making the dependency strictly one-directional.
- With only 2 production files below the 5-file measurement threshold, the extreme coupling score primarily reflects that both files are heavily imported externally rather than indicating design complexity that requires structural intervention.
- rex-core-utilities has zero internal imports between its two production files (keywords.ts and verify.ts) — the two files are connected to each other only through their shared consumer (rex-prd-engine), making this zone an artifact of graph proximity rather than a coherent module cluster.
- The 3 outbound imports from rex-core-utilities into rex-prd-engine combined with 8 inbound imports from rex-prd-engine suggest verify.ts depends on schema or tree types defined in the engine while also being consumed by the engine — a classic co-dependency loop where both files should live in the same zone.
- keywords.ts and verify.ts share no internal imports and exist as a zone solely because they both import from and are imported by rex-prd-engine — absorbing both files directly into rex-prd-engine would eliminate the zone, break the bidirectional cycle, and improve the engine's self-contained cohesion without adding external coupling.
- The 3 outbound imports from rex-core-utilities into rex-prd-engine (likely schema/type imports) combined with 8 inbound imports from the engine create a type-dependency loop — verify.ts or keywords.ts depend on types defined in the very zone that consumes them, which is structurally identical to a circular import even if the build currently resolves it.
- rex-core-utilities is the only zone in the codebase where both production files have zero internal imports at every level — source-to-source, test-to-test, and cross-file — making it a zone with no cohesive internal structure at any granularity.
- Absorbing keywords.ts and verify.ts into rex-prd-engine would increase the engine's file count by 2 while reducing the cross-zone edge count by 11 (3 outbound + 8 inbound), making this the highest structural yield per file moved in the entire rex package.
- keywords.ts likely provides keyword extraction for the analyze pipeline and verify.ts provides validation for the store layer — both are domain logic that was extracted into an under-populated zone rather than a genuine reusable utility boundary, which explains why the engine never stopped depending on them.
- rex-core-utilities has zero internal imports between its two production files and exists solely because both are co-imported by rex-prd-engine — it is a community-detection artifact, not an architectural boundary. Dissolving it by moving keywords.ts and verify.ts into rex-prd-engine eliminates 11 cross-zone edges and three governance alerts with no behavior change.
- rex-core-utilities is the only satellite zone in the rex package where inbound import count (8 from rex-prd-engine) dominates outbound (3 into rex-prd-engine) — a 1:2.67 out:in ratio is characteristic of library modules, not command handlers. Both other satellite zones (chunked-review: more outbound, prd-fix-command: 7 out vs 1 in) have outbound-dominant profiles consistent with CLI command consumers. This inverted ratio distinguishes keywords.ts and verify.ts as domain library code that was extracted out of the engine rather than CLI command code that grew upward into it — a fundamentally different structural pathology requiring absorption rather than inlining.
- hench-guard (10 files, cohesion 0.8) is the only satellite zone in the monorepo with more than 5 files and cohesion above the dual-fragility threshold — its structural health directly validates the 5-file reliable-metric floor: rex-core-utilities at 4 files (cohesion 0.18) and chunked-review at 5 files (cohesion 0.36) both sit at or below the threshold where cohesion scores become noise rather than signal, while hench-guard's 10 files produce a meaningful above-threshold reading.
- keywords.ts and verify.ts have an inbound-dominant import profile (8 in, 3 out from rex-prd-engine) that identifies them as library modules extracted from the engine, not CLI command satellites — the CLI-only content governance policy applied to other rex satellites does not apply here; document the distinction if absorption into rex-prd-engine is deferred to avoid applying the wrong governance rule to these files.
- [call graph] 67 internal calls, 2 outgoing, 5 incoming (cohesion: 0.97, coupling: 0.03)

</insights>
