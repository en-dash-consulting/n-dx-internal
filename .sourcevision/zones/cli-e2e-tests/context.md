# CLI End-to-End Tests — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: CLI End-to-End Tests (`cli-e2e-tests`)
Files: 18, Cohesion: 1.00, Coupling: 0.00
Risk: healthy (score: 0.00)
Description: Comprehensive end-to-end test suite validating all CLI commands, delegation paths, error handling, and architectural policy across the full monorepo.
Lines: 4940

</zone>

<files>

tests/e2e/architecture-policy.test.js (JavaScript, 140 lines, test)
tests/e2e/cli-arg-contracts.test.js (JavaScript, 320 lines, test)
tests/e2e/cli-ci.test.js (JavaScript, 445 lines, test)
tests/e2e/cli-config.test.js (JavaScript, 1044 lines, test)
tests/e2e/cli-delegation.test.js (JavaScript, 65 lines, test)
tests/e2e/cli-dev.test.js (JavaScript, 30 lines, test)
tests/e2e/cli-errors.test.js (JavaScript, 126 lines, test)
tests/e2e/cli-init.test.js (JavaScript, 264 lines, test)
tests/e2e/cli-orchestration.test.js (JavaScript, 401 lines, test)
tests/e2e/cli-pr-check.test.js (JavaScript, 325 lines, test)
tests/e2e/cli-refresh.test.js (JavaScript, 307 lines, test)
tests/e2e/cli-spawn-boundary.test.js (JavaScript, 169 lines, test)
tests/e2e/cli-start.test.js (JavaScript, 312 lines, test)
tests/e2e/cli-tool-resolution.test.js (JavaScript, 55 lines, test)
tests/e2e/cli-web.test.js (JavaScript, 321 lines, test)
tests/e2e/domain-isolation.test.js (JavaScript, 287 lines, test)
tests/e2e/e2e-helpers.js (JavaScript, 283 lines, test)
tests/e2e/verify-build.js (JavaScript, 46 lines, test)

</files>

<imports>

Internal:
  tests/e2e/cli-arg-contracts.test.js → tests/e2e/e2e-helpers.js {run}
  tests/e2e/cli-ci.test.js → tests/e2e/e2e-helpers.js {runBase, runResultBase, createTmpDir, removeTmpDir, setupRexDir, setupSourcevisionDir}
  tests/e2e/cli-delegation.test.js → tests/e2e/e2e-helpers.js {run, runFail}
  tests/e2e/cli-dev.test.js → tests/e2e/e2e-helpers.js {run, runResult, createTmpDir, removeTmpDir}
  tests/e2e/cli-errors.test.js → tests/e2e/e2e-helpers.js {run, runFail}

</imports>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[observation] [info] E2E tests spawn real CLI processes, so they are sensitive to build artifacts being stale; a pre-test build step or CI guard ensures test results reflect current source.
[observation] [info] The suite covers all major CLI surface areas (ci, config, delegation, dev, errors, init, plan, serve, sync, usage, work) providing strong regression coverage for the orchestration layer.
[observation] [info] architecture-policy.test.js and the gateway check scripts in viewer-gateway-tests both validate gateway rules — consider consolidating gateway policy assertions into one canonical location to avoid drift.
[anti-pattern] [warning] E2E tests that spawn CLI subprocesses share no documented workspace isolation strategy; if tests run concurrently and both write to .rex/prd.json or .sourcevision/, results are non-deterministic. No test uses a dedicated temp directory per test file.

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- At 18+ files covering init, config, delegation, errors, and architecture policy, this suite is the primary safety net for CLI regressions — its breadth is a strength
- architecture-policy.test.js acts as a meta-test that enforces structural rules (gateway compliance, circular dependency absence) at the E2E level, complementing the gateway contract tests in viewer-gateway-tests
- Zero coupling to production source means tests interact only through spawned CLI processes, which is correct for E2E but implies test helpers cannot easily share utilities with unit tests
- The suite covers all major CLI surface areas (ci, config, delegation, dev, errors, init, plan, serve, sync, usage, work) providing strong regression coverage for the orchestration layer.
- architecture-policy.test.js and the gateway check scripts in viewer-gateway-tests both validate gateway rules — consider consolidating gateway policy assertions into one canonical location to avoid drift.
- E2E tests spawn real CLI processes, so they are sensitive to build artifacts being stale; a pre-test build step or CI guard ensures test results reflect current source.
- cli-e2e-tests is the sole zone that exercises the full orchestration tier (cli.js, web.js, ci.js) via subprocess spawning — no other zone tests these entry points, making this zone a single point of regression coverage for the top architectural tier
- architecture-policy.test.js inside this zone acts as a structural meta-test; its scope overlaps with check-gateway-regex.mjs and check-gateway-test.mjs in viewer-gateway-tests, but they use different mechanisms (JS test runner vs standalone .mjs scripts), making it hard to know which is authoritative if they diverge
- The 18-file e2e suite spawns real CLI subprocesses that read and write shared directories (.sourcevision/, .rex/, .hench/); no documented test-isolation strategy (e.g., per-test temp directories) means parallel test execution could produce non-deterministic failures through shared artifact state
- cli-e2e-tests is the only zone that exercises cli.js, web.js, and ci.js — but those entry points delegate to spawned subprocesses that must have been built beforehand; the suite has no pre-test build guard, so a stale dist/ silently tests an older version of the code
- E2E tests that spawn CLI subprocesses share no documented workspace isolation strategy; if tests run concurrently and both write to .rex/prd.json or .sourcevision/, results are non-deterministic. No test uses a dedicated temp directory per test file.
- [call graph] 404 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
