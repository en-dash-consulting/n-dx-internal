# CLI E2E Tests — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: CLI E2E Tests (`cli-e2e-tests`)
Files: 19, Cohesion: 1.00, Coupling: 0.00
Risk: healthy (score: 0.00)
Description: End-to-end test suite for the n-dx CLI orchestration layer, covering argument contracts, delegation, spawn boundaries, and architectural policy enforcement.
Lines: 5210

</zone>

<files>

tests/e2e/architecture-policy.test.js (JavaScript, 226 lines, test)
tests/e2e/cli-arg-contracts.test.js (JavaScript, 320 lines, test)
tests/e2e/cli-ci.test.js (JavaScript, 445 lines, test)
tests/e2e/cli-config.test.js (JavaScript, 1044 lines, test)
tests/e2e/cli-contract.test.mjs (JavaScript, 103 lines, test)
tests/e2e/cli-delegation.test.js (JavaScript, 65 lines, test)
tests/e2e/cli-dev.test.js (JavaScript, 30 lines, test)
tests/e2e/cli-errors.test.js (JavaScript, 126 lines, test)
tests/e2e/cli-init.test.js (JavaScript, 264 lines, test)
tests/e2e/cli-orchestration.test.js (JavaScript, 401 lines, test)
tests/e2e/cli-pr-check.test.js (JavaScript, 325 lines, test)
tests/e2e/cli-refresh.test.js (JavaScript, 307 lines, test)
tests/e2e/cli-spawn-boundary.test.js (JavaScript, 169 lines, test)
tests/e2e/cli-start.test.js (JavaScript, 312 lines, test)
tests/e2e/cli-tool-resolution.test.js (JavaScript, 55 lines, test)
tests/e2e/cli-web.test.js (JavaScript, 321 lines, test)
tests/e2e/domain-isolation.test.js (JavaScript, 368 lines, test)
tests/e2e/e2e-helpers.js (JavaScript, 283 lines, test)
tests/e2e/verify-build.js (JavaScript, 46 lines, test)

</files>

<imports>

Internal:
  tests/e2e/cli-arg-contracts.test.js → tests/e2e/e2e-helpers.js {run}
  tests/e2e/cli-ci.test.js → tests/e2e/e2e-helpers.js {runBase, runResultBase, createTmpDir, removeTmpDir, setupRexDir, setupSourcevisionDir}
  tests/e2e/cli-delegation.test.js → tests/e2e/e2e-helpers.js {run, runFail}
  tests/e2e/cli-dev.test.js → tests/e2e/e2e-helpers.js {run, runResult, createTmpDir, removeTmpDir}
  tests/e2e/cli-errors.test.js → tests/e2e/e2e-helpers.js {run, runFail}

</imports>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[observation] [info] Having architecture-policy and domain-isolation tests co-located with behavioral CLI tests is an excellent practice — it makes architectural drift visible in the same CI run that validates behavior.
[observation] [info] The zone is purely test files with cohesion 1 and coupling 0, confirming correct isolation — no production source is accidentally mixed in.
[observation] [warning] cli-contract.test.mjs uses an .mjs extension while all 16 sibling test files use .js. This inconsistency can cause the file to be missed or handled differently by vitest glob patterns — normalize to .js or .ts.
[pattern] [info] E2E tests couple to production CLI packages exclusively through subprocess spawning, not imports. Import-graph coupling metrics are structurally blind to this dependency class — treat coupling 0 as 'not measurable' rather than 'no coupling' for this zone.
[pattern] [warning] cli-contract.test.mjs is present in the zone manifest but deleted in the working tree. The zone will silently misreport file count and cohesion until re-analyzed.
[anti-pattern] [info] The previously noted stale-file finding cites tests/cli-contract.test.mjs (root) as deleted while the zone contains tests/e2e/cli-contract.test.mjs (subdirectory). If these are distinct files, the zone manifest may be accurate and the stale-classification observation is a false alarm. A path-exact audit is required before acting on the deletion finding.
[suggestion] [info] All 19 e2e test files use .js (or .mjs) rather than .ts. Migrating to .ts would close the type-safety gap between tests and production CLIs, allowing TypeScript to catch contract breaks before runtime. At minimum, architecture-policy.test.js and domain-isolation.test.js — which enforce typed structural rules — are the highest-value candidates for TypeScript migration.
[suggestion] [info] Server-lifecycle e2e tests (cli-dev.test.js, any test that spawns a background server) should be tagged or placed in a subdirectory (e.g. tests/e2e/server/) separate from command e2e tests. This allows parallel execution of the command subset without port-conflict risk and makes flaky test isolation faster. The current flat structure hides this execution-model distinction.

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- architecture-policy.test.js and domain-isolation.test.js enforce the four-tier dependency hierarchy at runtime — this is a strong pattern that catches regressions automatically
- The suite covers all major CLI entry points (init, plan, work, status, config, dev, ci, web/start) providing broad behavioural confidence on the orchestration surface
- cli-contract.test.mjs uses .mjs extension while all peers use .js — the mixed extension pattern may cause inconsistent test runner discovery depending on vitest config
- cli-contract.test.mjs uses an .mjs extension while all 16 sibling test files use .js. This inconsistency can cause the file to be missed or handled differently by vitest glob patterns — normalize to .js or .ts.
- Having architecture-policy and domain-isolation tests co-located with behavioral CLI tests is an excellent practice — it makes architectural drift visible in the same CI run that validates behavior.
- The zone is purely test files with cohesion 1 and coupling 0, confirming correct isolation — no production source is accidentally mixed in.
- tests/cli-contract.test.mjs appears as a deleted file (D) in the working tree git status but is still listed in this zone — the zone classification is stale and will diverge from the actual file set on next analysis
- The e2e suite achieves coupling 0 via subprocess spawning (exec/spawn) rather than imports — static import-graph analysis structurally cannot see the runtime dependency on all four CLI entry points (cli.js, web.js, ci.js, config.js). Coupling 0 is technically correct but architecturally misleading for this zone type.
- cli-contract.test.mjs is present in the zone manifest but deleted in the working tree. The zone will silently misreport file count and cohesion until re-analyzed.
- E2E tests couple to production CLI packages exclusively through subprocess spawning, not imports. Import-graph coupling metrics are structurally blind to this dependency class — treat coupling 0 as 'not measurable' rather than 'no coupling' for this zone.
- The deleted file shown in git status is tests/cli-contract.test.mjs (root tests/ directory) while the zone manifest lists tests/e2e/cli-contract.test.mjs (tests/e2e/ subdirectory) — these are distinct paths. The existing insight conflates them; the e2e-subdirectory version may still exist, making the stale-classification concern either moot or different from what was previously described.
- The 414 internal call-graph edges with zero outgoing represent test-to-test and test-to-fixture calls only. The absence of any outgoing edges means that when a CLI entry point changes its contract, no static analysis signal propagates to this zone — breakage is only detectable at runtime, not at graph-analysis time.
- The previously noted stale-file finding cites tests/cli-contract.test.mjs (root) as deleted while the zone contains tests/e2e/cli-contract.test.mjs (subdirectory). If these are distinct files, the zone manifest may be accurate and the stale-classification observation is a false alarm. A path-exact audit is required before acting on the deletion finding.
- The suite mixes two structurally distinct test categories: short-lived command tests (init, status, next, config) that complete in milliseconds and long-running server process tests (cli-dev, cli-web/start) that must spawn and kill background processes. These two categories have different flakiness profiles — server tests risk port conflicts and timing races in parallel runs — but they are not separated or annotated in any way within the zone.
- The absence of TypeScript (.ts) across all 19 e2e test files while all production code is TypeScript creates a type-safety gap: e2e tests cannot catch type errors introduced in production CLI interfaces and will only fail at runtime when an API contract breaks. The gap is widest for architecture-policy.test.js and domain-isolation.test.js, which enforce structural rules but cannot express typed expectations about the import shapes they inspect.
- Server-lifecycle e2e tests (cli-dev.test.js, any test that spawns a background server) should be tagged or placed in a subdirectory (e.g. tests/e2e/server/) separate from command e2e tests. This allows parallel execution of the command subset without port-conflict risk and makes flaky test isolation faster. The current flat structure hides this execution-model distinction.
- All 19 e2e test files use .js (or .mjs) rather than .ts. Migrating to .ts would close the type-safety gap between tests and production CLIs, allowing TypeScript to catch contract breaks before runtime. At minimum, architecture-policy.test.js and domain-isolation.test.js — which enforce typed structural rules — are the highest-value candidates for TypeScript migration.
- [call graph] 414 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
