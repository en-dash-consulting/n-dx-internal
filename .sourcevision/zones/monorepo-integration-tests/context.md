# Monorepo Integration Tests — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Monorepo Integration Tests (`monorepo-integration-tests`)
Files: 2, Cohesion: 1.00, Coupling: 0.00
Risk: healthy (score: 0.00)
Description: Validates cross-package API contracts and scheduler startup behavior at the monorepo boundary, independent of any single package's test suite.
Lines: 685

</zone>

<files>

tests/integration/cross-package-contracts.test.js (JavaScript, 336 lines, test)
tests/integration/scheduler-startup.test.js (JavaScript, 349 lines, test)

</files>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[observation] [info] Only 2 integration test files exist at the monorepo root; as the number of cross-package interactions grows, this zone should grow proportionally to maintain contract coverage.
[observation] [info] Zero coupling confirms these tests import nothing from shared test helpers — keeping them self-contained avoids fragile test-infrastructure dependencies.
[relationship] [info] scheduler-startup.test.js in monorepo-integration-tests partially overlaps the concern of task-usage-analytics scheduler tests — confirm there is no test duplication and that the integration test validates cross-package startup ordering rather than internal scheduler logic
[anti-pattern] [warning] Only 2 integration test files exist at the monorepo boundary. As cross-package interactions grow (rex↔sourcevision data flow, hench↔rex task selection, web↔rex MCP contract), the integration ring has no mechanism to enforce proportional growth — unlike the e2e suite which has an architecture-policy guard. A test coverage policy for cross-package contracts is absent.
[suggestion] [warning] Add an integration test for the HTTP MCP transport contract: verify that /mcp/rex and /mcp/sourcevision endpoints respond correctly, that Mcp-Session-Id header is returned on session creation, and that tool calls return valid JSON-RPC responses. This contract currently has zero test coverage at the integration boundary.
[suggestion] [warning] Add an integration test for the rex analyze → sourcevision output consumption flow: verify that rex's analyze command correctly reads and parses .sourcevision/CONTEXT.md and inventory files produced by sourcevision analyze. This is the highest-frequency cross-package data contract and is currently untested at the integration level.

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- Perfect cohesion and zero coupling confirm this is a fully isolated test zone with no shared infrastructure bleeding in
- Two files covering cross-package contracts and scheduler startup are a thin but critical safety net — expanding coverage for other cross-package integration points (e.g. rex↔sourcevision data flow) would strengthen the monorepo contract surface
- Because these tests live at the repo root rather than inside a package, they are the right place to catch regressions that individual package test suites cannot see
- Only 2 integration test files exist at the monorepo root; as the number of cross-package interactions grows, this zone should grow proportionally to maintain contract coverage.
- Zero coupling confirms these tests import nothing from shared test helpers — keeping them self-contained avoids fragile test-infrastructure dependencies.
- monorepo-integration-tests and monorepo-e2e-tests are correctly stratified: integration tests verify cross-package data contracts while e2e tests verify CLI behavior — they never import from each other, preserving clear test-layer separation
- scheduler-startup.test.js in monorepo-integration-tests partially overlaps the concern of task-usage-analytics scheduler tests — confirm there is no test duplication and that the integration test validates cross-package startup ordering rather than internal scheduler logic
- scheduler-startup.test.js and usage-cleanup-scheduler.test.ts (in task-usage-analytics) risk testing the same scheduler lifecycle from two different angles without a documented boundary between them. If both assert on the same observable behavior, one is redundant; if they test complementary concerns, that boundary should be documented.
- Only 2 integration test files exist at the monorepo boundary. As cross-package interactions grow (rex↔sourcevision data flow, hench↔rex task selection, web↔rex MCP contract), the integration ring has no mechanism to enforce proportional growth — unlike the e2e suite which has an architecture-policy guard. A test coverage policy for cross-package contracts is absent.
- The HTTP MCP transport layer (added per CLAUDE.md documentation: /mcp/rex and /mcp/sourcevision endpoints with session management) has no integration test coverage in this zone. The scheduler-startup.test.js and cross-package-contracts.test.js predate the HTTP transport and do not validate MCP session lifecycle, endpoint availability, or streamable HTTP handshake — a new class of cross-package contract with no test coverage.
- With only 2 files, this zone has no test for the rex↔sourcevision data flow (e.g. analyze pipeline consuming sourcevision output) or the hench↔rex task-selection contract — the two highest-frequency cross-package interactions in the codebase.
- Add an integration test for the HTTP MCP transport contract: verify that /mcp/rex and /mcp/sourcevision endpoints respond correctly, that Mcp-Session-Id header is returned on session creation, and that tool calls return valid JSON-RPC responses. This contract currently has zero test coverage at the integration boundary.
- Add an integration test for the rex analyze → sourcevision output consumption flow: verify that rex's analyze command correctly reads and parses .sourcevision/CONTEXT.md and inventory files produced by sourcevision analyze. This is the highest-frequency cross-package data contract and is currently untested at the integration level.
- [call graph] 15 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
