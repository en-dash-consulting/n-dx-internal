# Landing Page — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Landing Page (`landing-page`)
Files: 3, Cohesion: 1.00, Coupling: 0.00
Risk: healthy (score: 0.00)
Description: Self-contained static landing page consisting of an HTML entry point, scoped stylesheet, and minimal TypeScript initialization logic.
Lines: 1941

</zone>

<files>

packages/web/src/landing/index.html (HTML, 309 lines, other)
packages/web/src/landing/landing.css (CSS, 1317 lines, other)
packages/web/src/landing/landing.ts (TypeScript, 315 lines, source)

</files>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[suggestion] [info] Reclassify landing.ts from 'service' to 'entrypoint' archetype to prevent sourcevision health heuristics from applying service-zone analysis expectations to DOM initialization code. The 'service' classification is the only such outlier in a zero-coupling static zone in the entire codebase and will produce false positives in any automated zone health scoring that uses archetype to set per-zone quality thresholds.

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- Perfect cohesion and zero coupling make this zone an ideal candidate to remain unchanged during refactors to the main viewer or server zones.
- With only 3 files, this zone is deliberately minimal — any business logic added here would be an architectural smell worth flagging.
- The isolation from the main viewer zone suggests landing and app routing are cleanly separated at the entry-point level.
- Zero coupling and perfect cohesion confirm the landing page is correctly treated as a standalone static artifact, not entangled with app state or viewer logic.
- The landing TypeScript file classified as a 'service' is worth reviewing — landing pages typically need minimal logic, so any service-layer concerns here may indicate scope creep.
- landing-page and viewer-static-assets are both zero-coupling zones within the same package as web-dashboard-mcp-server — together they form a 'shell isolation' pattern where entry-point assets are cleanly partitioned from the app runtime at the module graph level.
- landing.ts is classified as a 'service' archetype in a zone that is otherwise entirely static (HTML, CSS). A service-archetype file in a zero-coupling, zero-business-logic zone suggests the classifier mapped it to service because it contains JavaScript initialization code rather than React components — but this ambiguity means the classification is misleading and could mask real service-layer logic being added here without review.
- landing.ts carries a 'service' archetype classification despite living in a static landing zone with zero coupling. If this classification reflects actual service-layer logic (API calls, state management, event orchestration) rather than simple DOM initialization, that logic is invisible to zone-level coupling metrics because the zone has no outbound imports. Audit landing.ts to confirm it contains only DOM bootstrap code; any service concerns found should be extracted to the main server or viewer zones where they can be properly tracked.
- landing.ts is the only file in any zero-coupling, 3-or-fewer-file zone across the entire codebase to carry a 'service' archetype classification. All other zero-coupling minimal zones (viewer-static-assets, prd-tree-lifecycle) contain no service-classified files. If sourcevision's health heuristics apply service-zone thresholds (expected test coverage, API boundary checks, error handling expectations) to landing.ts, the landing-page zone will systematically fail heuristic checks designed for backend services despite containing only DOM bootstrap code.
- Reclassify landing.ts from 'service' to 'entrypoint' archetype to prevent sourcevision health heuristics from applying service-zone analysis expectations to DOM initialization code. The 'service' classification is the only such outlier in a zero-coupling static zone in the entire codebase and will produce false positives in any automated zone health scoring that uses archetype to set per-zone quality thresholds.
- [call graph] 10 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
