# Web Viewer Isolated Unit Tests — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Web Viewer Isolated Unit Tests (`web-viewer-unit-tests`)
Files: 2, Cohesion: 0.00, Coupling: 1.00
Risk: catastrophic (score: 1.00)
Description: A pair of orphaned unit test files for viewer accessibility and status-filter behaviour that share no internal import edges.
Entry points: packages/web/src/viewer/components/guide.ts, packages/web/src/viewer/components/prd-tree/status-filter.ts
Lines: 658

</zone>

<files>

packages/web/tests/unit/viewer/accessibility.test.ts (TypeScript, 401 lines, test)
packages/web/tests/unit/viewer/status-filter.test.ts (TypeScript, 257 lines, test)

</files>

<imports>


Outgoing (this zone → other zones):
  → web-viewer: packages/web/tests/unit/viewer/accessibility.test.ts → packages/web/src/viewer/components/guide.ts; packages/web/tests/unit/viewer/accessibility.test.ts → packages/web/src/viewer/components/prd-tree/prd-tree.ts; packages/web/tests/unit/viewer/accessibility.test.ts → packages/web/src/viewer/components/prd-tree/status-filter.ts; packages/web/tests/unit/viewer/accessibility.test.ts → packages/web/src/viewer/components/prd-tree/types.ts; packages/web/tests/unit/viewer/accessibility.test.ts → packages/web/src/viewer/components/theme-toggle.ts; packages/web/tests/unit/viewer/status-filter.test.ts → packages/web/src/viewer/components/prd-tree/status-filter.ts; packages/web/tests/unit/viewer/status-filter.test.ts → packages/web/src/viewer/components/prd-tree/types.ts

</imports>

<findings>

[observation] [warning] High coupling (1) — 7 imports target "web-viewer"
[observation] [warning] Both test files target components that live in the Web Viewer Hub zone; moving them into packages/web/tests/unit/viewer alongside peer viewer tests would improve discoverability and eliminate the orphan zone.
[observation] [warning] Cohesion of 0 and coupling of 1 means the community-detection algorithm grouped these files by elimination rather than affinity — they have no shared internal imports.
[observation] [info] This zone contains only test files, so the '-tests' suffix is applied correctly and tooling will not misclassify any production code.
[pattern] [info] This zone is a community-detection artifact with no structural significance. Its elimination requires moving two files: accessibility.test.ts and status-filter.test.ts into the existing packages/web/tests/unit/viewer/ directory. No import changes are needed — the zone disappears as a side effect of file relocation.
[suggestion] [info] Treat the zone name 'web-viewer-unit-tests' as a self-documenting migration ticket: the name itself encodes where the files should go. When accessibility.test.ts and status-filter.test.ts are moved into packages/web/tests/unit/viewer/, the zone dissolves and the name becomes irrelevant — prioritizing this mechanical move eliminates the zone before the embedded intent degrades into confusion.
[suggestion] [info] Zone "Web Viewer Isolated Unit Tests" (web-viewer-unit-tests) has catastrophic risk (score: 1.00, cohesion: 0.00, coupling: 1.00) — unreliable: zone has only 2 files (minimum 5 for reliable metrics)

</findings>

<insights>

- High coupling (1) — 7 imports target "web-viewer"
- Zero cohesion indicates these two test files were not clustered by any shared internal imports — they are effectively isolated from each other.
- Both tests exercise production code that belongs to the Web Viewer Hub zone, suggesting they could be co-located with that zone's test suite.
- No action is required for correctness, but consolidating them into the web-viewer test directory would eliminate this ghost zone.
- Cohesion of 0 and coupling of 1 means the community-detection algorithm grouped these files by elimination rather than affinity — they have no shared internal imports.
- Both test files target components that live in the Web Viewer Hub zone; moving them into packages/web/tests/unit/viewer alongside peer viewer tests would improve discoverability and eliminate the orphan zone.
- This zone contains only test files, so the '-tests' suffix is applied correctly and tooling will not misclassify any production code.
- The two orphan test files (accessibility.test.ts, status-filter.test.ts) exercise production code in web-viewer but were placed outside the co-location convention. Their zero cohesion is a direct consequence of having no shared imports with each other — they are unrelated files grouped by elimination. The fix is purely mechanical: moving them into packages/web/tests/unit/viewer/ alongside peer viewer tests eliminates this zone with no code changes.
- This zone is a community-detection artifact with no structural significance. Its elimination requires moving two files: accessibility.test.ts and status-filter.test.ts into the existing packages/web/tests/unit/viewer/ directory. No import changes are needed — the zone disappears as a side effect of file relocation.
- This orphan zone's continued existence as a named zone artificially inflates the monorepo's zone count and cross-zone edge count in sourcevision output, making the web package appear more structurally fragmented than it is. Its elimination reduces false-positive governance alerts and improves the signal-to-noise ratio of future zone health reports — a metric hygiene argument for prioritizing the file move beyond structural cleanliness alone.
- The zone name 'web-viewer-unit-tests' embeds the name of the target zone ('web-viewer') in a way that documents intent (these tests belong to web-viewer) but does not enforce it. If the zone persists after file moves, the name becomes stale metadata — a naming pattern that documents a desired future state rather than a current structural reality, which is uncommon among zone names in this codebase.
- Treat the zone name 'web-viewer-unit-tests' as a self-documenting migration ticket: the name itself encodes where the files should go. When accessibility.test.ts and status-filter.test.ts are moved into packages/web/tests/unit/viewer/, the zone dissolves and the name becomes irrelevant — prioritizing this mechanical move eliminates the zone before the embedded intent degrades into confusion.
- [call graph] 42 internal calls, 23 outgoing, 0 incoming (cohesion: 0.65, coupling: 0.35)

</insights>
