# Crash Recovery Subsystem — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Crash Recovery Subsystem (`crash-recovery`)
Files: 5, Cohesion: 0.29, Coupling: 0.71
Risk: catastrophic (score: 0.71)
Description: The web viewer's crash detection and graceful recovery subsystem, comprising a crash-detector utility and a React hook that restores viewer state after unhandled errors.
Entry points: packages/web/src/viewer/hooks/use-crash-recovery.ts, packages/web/src/viewer/performance/crash-detector.ts
Lines: 994

</zone>

<files>

packages/web/src/viewer/hooks/use-crash-recovery.ts (TypeScript, 134 lines, source)
packages/web/src/viewer/performance/crash-detector.ts (TypeScript, 297 lines, source)
packages/web/tests/helpers/crash-detector-test-support.ts (TypeScript, 20 lines, test)
packages/web/tests/unit/viewer/crash-detector.test.ts (TypeScript, 275 lines, test)
packages/web/tests/unit/viewer/use-crash-recovery.test.ts (TypeScript, 268 lines, test)

</files>

<imports>

Internal:
  packages/web/src/viewer/hooks/use-crash-recovery.ts → packages/web/src/viewer/performance/crash-detector.ts {detectCrash, saveNavigationState, clearSavedNavigationState, markRecoveryShown, wasRecoveryShown, resetCrashDetector}
  packages/web/tests/unit/viewer/crash-detector.test.ts → packages/web/src/viewer/performance/crash-detector.ts {detectCrash, saveNavigationState, clearSavedNavigationState, markRecoveryShown, wasRecoveryShown, getDetectionResult, clearCrashHistory, resetCrashDetector}
  packages/web/tests/unit/viewer/crash-detector.test.ts → packages/web/src/viewer/performance/crash-detector.ts {CrashDetectionResult, SavedNavigationState}
  packages/web/tests/unit/viewer/crash-detector.test.ts → packages/web/tests/helpers/crash-detector-test-support.ts {HEARTBEAT_KEY, NAV_STATE_KEY, CRASH_HISTORY_KEY, RECOVERY_SHOWN_KEY, CRASH_LOOP_WINDOW_MS, CRASH_LOOP_THRESHOLD, MAX_CRASH_HISTORY}
  packages/web/tests/unit/viewer/use-crash-recovery.test.ts → packages/web/src/viewer/hooks/use-crash-recovery.ts {useCrashRecovery}
  packages/web/tests/unit/viewer/use-crash-recovery.test.ts → packages/web/src/viewer/hooks/use-crash-recovery.ts {UseCrashRecoveryResult}
  packages/web/tests/unit/viewer/use-crash-recovery.test.ts → packages/web/src/viewer/performance/crash-detector.ts {CrashDetectionResult, SavedNavigationState}

Outgoing (this zone → other zones):
  → web-dashboard: packages/web/src/viewer/hooks/use-crash-recovery.ts → packages/web/src/viewer/performance/index.ts; packages/web/src/viewer/hooks/use-crash-recovery.ts → packages/web/src/viewer/types.ts; packages/web/src/viewer/performance/crash-detector.ts → packages/web/src/viewer/types.ts

Incoming (other zones → this zone):
  ← web-dashboard: packages/web/src/viewer/main.ts → packages/web/src/viewer/hooks/use-crash-recovery.ts; packages/web/src/viewer/performance/index.ts → packages/web/src/viewer/performance/crash-detector.ts; packages/web/tests/unit/viewer/crash-recovery-banner.test.ts → packages/web/src/viewer/performance/crash-detector.ts

</imports>

<findings>

[observation] [warning] High coupling (0.71) — 3 imports target "web-dashboard"
[observation] [warning] Low cohesion (0.29) — files are loosely related, consider splitting this zone
[observation] [warning] Cohesion 0.29 is below the 0.4 threshold, driven by the small file count and the fact that crash-detector.ts and use-crash-recovery.ts have different primary importers; this is acceptable for a two-file subsystem.
[observation] [warning] Coupling 0.71 exceeds the 0.6 warning threshold; this is an artifact of the bidirectional composition-root wiring with web-dashboard documented in zone hints rather than uncontrolled dependency spread.
[observation] [info] Isolating crash detection into its own zone prevents error-handling concerns from leaking into viewer component logic — a clean separation of the unhappy path.
[pattern] [warning] The crash-recovery zone exhibits the 'forced satellite' anti-pattern: two files that don't directly import each other are grouped together because they share a thematic concern (crash handling) rather than an import relationship. The low cohesion (0.29) is a direct consequence. If this zone were dissolved and both files pinned to web-dashboard, the cohesion metric would disappear but architectural clarity would improve.
[relationship] [warning] crash-recovery ↔ web-dashboard bidirectional coupling (3 imports each direction) creates a small import cycle. Unlike the mcp-route-layer, which has a type-only reverse edge, crash-recovery's reverse imports are runtime (use-crash-recovery.ts hooks back into viewer state). This makes crash-recovery the only zone in this batch with a true runtime circular dependency with web-dashboard, even if intentional.
[anti-pattern] [warning] The runtime bidirectional dependency between crash-recovery and web-dashboard (3 imports each direction) has no interface boundary — crash-recovery directly imports from web-dashboard viewer state and web-dashboard directly imports crash-detector. If crash handling ever needs to support server-side error boundaries (process signals, middleware), the viewer-coupled implementation cannot be reused without extracting a shared interface, and the coupling surface would have to expand into a new zone simultaneously.
[suggestion] [info] Rename zone from 'crash-recovery' to 'web-crash-recovery' to align with the 'web-' prefix convention and enable reliable package-level zone filtering.
[suggestion] [info] crash-detector.ts uses only sessionStorage and has zero framework dependencies — it could be reused in a Node.js or alternative-framework context without modification. Separating it from the Preact-coupled use-crash-recovery.ts (e.g. pinning crash-detector.ts to a platform-utilities zone) would make its portability explicit and prevent it from acquiring accidental Preact coupling via zone co-location.
[suggestion] [critical] Zone "Crash Recovery Subsystem" (crash-recovery) has catastrophic risk (score: 0.71, cohesion: 0.29, coupling: 0.71) — requires immediate architectural intervention

</findings>

<insights>

- Low cohesion (0.29) — files are loosely related, consider splitting this zone
- High coupling (0.71) — 3 imports target "web-dashboard"
- Coupling of 0.71 exceeds the warning threshold but reflects a deliberate design: crash-recovery hooks back into web-dashboard viewer state and web-dashboard wires the crash-detector at the composition root — both directions are intentional.
- The zone's two production files (crash-detector.ts, use-crash-recovery.ts) have matching dedicated tests, confirming the subsystem is well-tested relative to its size.
- The small, focused scope (2 production files, 3 test files) is a positive signal — crash handling is isolated from the rest of the viewer rather than scattered across components.
- Coupling 0.71 exceeds the 0.6 warning threshold; this is an artifact of the bidirectional composition-root wiring with web-dashboard documented in zone hints rather than uncontrolled dependency spread.
- Cohesion 0.29 is below the 0.4 threshold, driven by the small file count and the fact that crash-detector.ts and use-crash-recovery.ts have different primary importers; this is acceptable for a two-file subsystem.
- Isolating crash detection into its own zone prevents error-handling concerns from leaking into viewer component logic — a clean separation of the unhappy path.
- The combination of low cohesion (0.29) and high coupling (0.71) in a 5-file zone is a structural signal that this zone may be too small to stand alone — its two production files have different primary importers within web-dashboard, weakening internal cohesion while maximizing external coupling.
- crash-detector.ts and use-crash-recovery.ts serve complementary but distinct roles (detection vs. recovery state), which is the root cause of the low cohesion metric; they are coupled to each other only indirectly through web-dashboard's composition root.
- The crash-recovery zone exhibits the 'forced satellite' anti-pattern: two files that don't directly import each other are grouped together because they share a thematic concern (crash handling) rather than an import relationship. The low cohesion (0.29) is a direct consequence. If this zone were dissolved and both files pinned to web-dashboard, the cohesion metric would disappear but architectural clarity would improve.
- crash-recovery ↔ web-dashboard bidirectional coupling (3 imports each direction) creates a small import cycle. Unlike the mcp-route-layer, which has a type-only reverse edge, crash-recovery's reverse imports are runtime (use-crash-recovery.ts hooks back into viewer state). This makes crash-recovery the only zone in this batch with a true runtime circular dependency with web-dashboard, even if intentional.
- crash-detector.ts and use-crash-recovery.ts have no direct import edge between them — all coupling passes through web-dashboard as an intermediary. This means the zone's internal cohesion will never improve without restructuring how crash detection signals flow to recovery state, because the two files are only loosely 'crash-related' rather than architecturally coupled to each other
- The three-file test suite (crash-detector.test.ts, use-crash-recovery.test.ts, crash-detector-test-support.ts) is larger by file count than the two-file production surface it covers; the shared test helper (crash-detector-test-support.ts) implicitly documents a cross-file dependency that does not exist in production code, further reducing cohesion signal reliability
- The runtime bidirectional dependency between crash-recovery and web-dashboard (3 imports each direction) has no interface boundary — crash-recovery directly imports from web-dashboard viewer state and web-dashboard directly imports crash-detector. If crash handling ever needs to support server-side error boundaries (process signals, middleware), the viewer-coupled implementation cannot be reused without extracting a shared interface, and the coupling surface would have to expand into a new zone simultaneously.
- use-crash-recovery.ts is a Preact framework hook (imports from preact/hooks, manages component state) while crash-detector.ts is framework-agnostic and uses only browser platform APIs (sessionStorage). Grouping a framework-coupled hook with a framework-free platform utility conflates the Preact component layer with the browser platform layer — crash-detector.ts is portable to any framework or server-side context but its placement alongside a Preact hook obscures this portability.
- Zone name 'crash-recovery' lacks the 'web-' prefix used by peer web-package zones (web-dashboard, web-landing-scripts, web-static-assets), creating the same package-prefix filtering gap as mcp-route-layer.
- crash-detector.ts uses only sessionStorage and has zero framework dependencies — it could be reused in a Node.js or alternative-framework context without modification. Separating it from the Preact-coupled use-crash-recovery.ts (e.g. pinning crash-detector.ts to a platform-utilities zone) would make its portability explicit and prevent it from acquiring accidental Preact coupling via zone co-location.
- Rename zone from 'crash-recovery' to 'web-crash-recovery' to align with the 'web-' prefix convention and enable reliable package-level zone filtering.
- [call graph] 81 internal calls, 0 outgoing, 1 incoming (cohesion: 1, coupling: 0)

</insights>
