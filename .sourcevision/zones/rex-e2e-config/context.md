# Rex E2E Config — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Rex E2E Config (`rex-e2e-config`)
Files: 8, Cohesion: 0.00, Coupling: 0.00
Risk: at-risk (score: 0.50)
Description: Rex package-level e2e test suite configuration, test runner setup, and generated artifacts that sit outside the main source and unit-test zones.
Lines: 1263

</zone>

<files>

packages/rex/llms.txt (Text, 62 lines, docs)
packages/rex/tests/e2e/cli-quiet.test.ts (TypeScript, 102 lines, test)
packages/rex/tests/e2e/cli-recommend.test.ts (TypeScript, 122 lines, test)
packages/rex/tests/e2e/cli-smart-add.test.ts (TypeScript, 722 lines, test)
packages/rex/tests/e2e/cli-sync.test.ts (TypeScript, 66 lines, test)
packages/rex/tests/e2e/cli-workflow.test.ts (TypeScript, 167 lines, test)
packages/rex/tests/e2e/fixtures/sample-prd/.rex/execution-log.jsonl (Other, 3 lines, test)
packages/rex/vitest.config.ts (TypeScript, 19 lines, config)

</files>

<findings>

[observation] [info] Isolated files — no import edges between 8 files, cohesion is unmeasurable (reported as 0)
[observation] [warning] Cohesion 0 is a warning signal: these 8 files share no import edges with each other, meaning community detection has grouped package-root artifacts by proximity rather than relationship. This zone likely should not exist as a standalone zone — its contents belong in rex-e2e (test files) or be excluded from zone analysis (generated files, config).
[observation] [info] The e2e test files here (cli-sync, cli-recommend, cli-smart-add) sit outside the main rex-e2e zone (cohesion 1.0, 4 files) — consolidating all rex e2e tests into one zone would eliminate this low-cohesion artifact and simplify zone governance.
[observation] [info] packages/rex/llms.txt is a generated sourcevision output artifact, not source code; including it in zone analysis skews metrics and should be excluded via the sourcevision ignore list alongside other generated files.
[pattern] [warning] rex-e2e-config (cohesion 0) and rex-e2e (cohesion 1, 4 files) represent a split e2e test zone that should be a single cohesive zone — the two zones together cover the same CLI surface but are separated by community detection grouping package-root artifacts with unrelated test files; consolidation would produce one high-cohesion e2e zone and eliminate the cohesion-0 artifact.
[suggestion] [info] Zone "Rex E2E Config" (rex-e2e-config) has at-risk risk (score: 0.50, cohesion: 0.00, coupling: 0.00) — approaching architectural risk thresholds

</findings>

<insights>

- Isolated files — no import edges between 8 files, cohesion is unmeasurable (reported as 0)
- Cohesion 0 across 8 loosely related files (generated llms.txt, vitest config, e2e test files, and a fixture) reflects that this zone is an artifact of community detection grouping package-root stragglers rather than a genuine architectural zone
- The e2e tests here (cli-quiet, cli-recommend, cli-smart-add, cli-sync, cli-workflow) complement the dedicated rex-e2e zone — audit whether they duplicate coverage or test different CLI surface areas
- The fixture file (sample-prd/.rex/execution-log.jsonl) co-located with test configs suggests the fixture directory may need its own explicit zone boundary to prevent test-infrastructure files from inflating production zone metrics
- Cohesion 0 is a warning signal: these 8 files share no import edges with each other, meaning community detection has grouped package-root artifacts by proximity rather than relationship. This zone likely should not exist as a standalone zone — its contents belong in rex-e2e (test files) or be excluded from zone analysis (generated files, config).
- packages/rex/llms.txt is a generated sourcevision output artifact, not source code; including it in zone analysis skews metrics and should be excluded via the sourcevision ignore list alongside other generated files.
- The e2e test files here (cli-sync, cli-recommend, cli-smart-add) sit outside the main rex-e2e zone (cohesion 1.0, 4 files) — consolidating all rex e2e tests into one zone would eliminate this low-cohesion artifact and simplify zone governance.
- rex-e2e-config (cohesion 0) and rex-e2e (cohesion 1, 4 files) represent a split e2e test zone that should be a single cohesive zone — the two zones together cover the same CLI surface but are separated by community detection grouping package-root artifacts with unrelated test files; consolidation would produce one high-cohesion e2e zone and eliminate the cohesion-0 artifact.
- The rex-e2e-config zone's cohesion-0 reading is confirmed: community detection grouped these 8 files by proximity (package root artifacts) despite having no import relationships. This is structurally identical to the web-unit zone (also cohesion 0) — both are test/config orphan zones created by community detection's proximity bias when files lack import edges to either of the two nearest well-connected clusters.
- [call graph] 110 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
