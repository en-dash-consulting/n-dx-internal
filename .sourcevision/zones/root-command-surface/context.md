# Unified CLI Surface — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Unified CLI Surface (`root-command-surface`)
Files: 6, Cohesion: 1.00, Coupling: 0.00
Description: Aggregates the repo’s primary node-based entrypoints, exposing config, help text, and shared tooling for CI and local use.
Lines: 3276

</zone>

<files>

ci.js (JavaScript, 269 lines, source)
cli.js (JavaScript, 473 lines, source)
config.js (JavaScript, 1003 lines, source)
help.js (JavaScript, 870 lines, source)
tests/unit/help.test.js (JavaScript, 252 lines, test)
web.js (JavaScript, 409 lines, source)

</files>

<imports>

Internal:
  cli.js → ci.js {runCI}
  cli.js → config.js {runConfig}
  cli.js → help.js {formatTypoSuggestion, getOrchestratorCommands, searchHelp, formatSearchResults, formatToolHelp, formatMainHelp, formatOrchestratorCommandHelp}
  cli.js → web.js {runWeb}
  tests/unit/help.test.js → help.js {editDistance, suggestCommands, formatTypoSuggestion, searchHelp, formatSearchResults, getOrchestratorCommands, getToolSubcommands, formatToolHelp, getRelatedCommands, formatRelatedCommands}

</imports>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[observation] [info] Centralize the three entrypoints (`ci.js`,`cli.js`,`web.js`) behind shared config/helpers to avoid drift in flag handling.
[observation] [info] Consider documenting how config.js keys map onto the three executables to simplify onboarding since this zone is the first touchpoint.
[observation] [info] Existing help tests (`tests/unit/help.test.js`) keep UX stable—extend them to cover CLI error paths for better regression protection.

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- Centralizes CI/CLI entrypoints around shared config and utilities.
- Has dedicated tests around help output, signalling maturity for user guidance.
- Centralize the three entrypoints (`ci.js`,`cli.js`,`web.js`) behind shared config/helpers to avoid drift in flag handling.
- Existing help tests (`tests/unit/help.test.js`) keep UX stable—extend them to cover CLI error paths for better regression protection.
- Consider documenting how config.js keys map onto the three executables to simplify onboarding since this zone is the first touchpoint.
- [call graph] 294 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
