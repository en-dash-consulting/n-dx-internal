# Rex Runtime State — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Rex Runtime State (`rex-runtime-state`)
Files: 8, Cohesion: 1.00, Coupling: 0.00
Risk: healthy (score: 0.00)
Description: Generated runtime data files managed by the rex PRD engine, including the live PRD tree, execution logs, and pending proposals.
Lines: 25295

</zone>

<files>

.rex/acknowledged-findings.json (JSON, 33 lines, other)
.rex/archive.json (JSON, 840 lines, other)
.rex/config.json (JSON, 6 lines, other)
.rex/execution-log.1.jsonl (Other, 3899 lines, other)
.rex/execution-log.jsonl (Other, 392 lines, other)
.rex/pending-proposals.json (JSON, 818 lines, other)
.rex/prd.json (JSON, 19289 lines, other)
.rex/workflow.md (Markdown, 18 lines, docs)

</files>

<findings>

[observation] [info] High cohesion (1) — files are tightly interconnected
[observation] [info] acknowledged-findings.json and pending-proposals.json represent workflow-phase state; if these accumulate indefinitely they may grow stale — a periodic pruning strategy would reduce noise.
[observation] [info] archive.json alongside prd.json implies a soft-delete pattern for removed PRD items, which is good for auditability and recovery.
[observation] [info] workflow.md is human-readable state that duplicates information from prd.json; ensure it is always regenerated from the canonical JSON source to avoid drift.
[anti-pattern] [warning] packages-rex:unit-analyze ↔ packages-rex:cli shows 239+100 bidirectional call-graph edges within the rex package. This internal bidirectional coupling mirrors the web↔web-viewer pattern and risks the same logical entanglement: changes to cli layer can ripple into unit-analyze and vice versa with no clear dependency direction.
[suggestion] [info] The suffix-numeric log rotation scheme (.1.jsonl, .2.jsonl) has no documented or enforced upper bound. Add a MAX_LOG_SEGMENTS constant to the rex package and enforce cleanup of segments beyond the limit during every rotation event to prevent unbounded .rex/ directory growth in long-running projects.
[suggestion] [info] workflow.md has no enforced regeneration trigger — it is the only human-readable file in a zone of machine-written artifacts, and its staleness is silently indistinguishable. Either (a) regenerate workflow.md on every rex write operation or (b) delete workflow.md and link users to 'rex status' for the human-readable view, eliminating the dual-source risk entirely.

</findings>

<insights>

- High cohesion (1) — files are tightly interconnected
- These are data artifacts, not source code — perfect cohesion (1.0) and zero coupling reflect that they are written and read exclusively through the rex package API.
- The presence of both execution-log.jsonl and execution-log.1.jsonl suggests log rotation is active; tooling that reads these files should handle multiple log segments.
- Keeping runtime state in a dedicated .rex/ directory (parallel to .sourcevision/ and .hench/) is a consistent pattern across the toolkit that makes backup and .gitignore management straightforward.
- acknowledged-findings.json and pending-proposals.json represent workflow-phase state; if these accumulate indefinitely they may grow stale — a periodic pruning strategy would reduce noise.
- archive.json alongside prd.json implies a soft-delete pattern for removed PRD items, which is good for auditability and recovery.
- workflow.md is human-readable state that duplicates information from prd.json; ensure it is always regenerated from the canonical JSON source to avoid drift.
- The bidirectional call-graph relationship between packages-rex:unit-analyze and packages-rex:cli (239 calls outgoing, 100 calls incoming) mirrors the web↔web-viewer bidirectional pattern at the inter-package level — suggesting bidirectional coupling is a recurring structural smell in this codebase, not an isolated web-package anomaly.
- packages-rex:unit-analyze ↔ packages-rex:cli shows 239+100 bidirectional call-graph edges within the rex package. This internal bidirectional coupling mirrors the web↔web-viewer pattern and risks the same logical entanglement: changes to cli layer can ripple into unit-analyze and vice versa with no clear dependency direction.
- The rotated log file uses suffix-numeric naming (.jsonl → .1.jsonl), implying an unbounded rotation scheme: as rotation events accumulate, files .1.jsonl, .2.jsonl, .3.jsonl will appear with no documented maximum count or total size cap. Without a retention policy enforced by the rex package, the .rex/ directory will grow unboundedly during long-running projects.
- The zone contains both machine-written files (prd.json, execution-log.jsonl, archive.json) and one human-readable file (workflow.md). This mixed authorship means the zone has two different consistency mechanisms: prd.json is the canonical source of truth enforced by code, while workflow.md is a derived human-readable view with no enforced regeneration trigger. A stale workflow.md is indistinguishable from a current one without diffing against prd.json.
- The suffix-numeric log rotation scheme (.1.jsonl, .2.jsonl) has no documented or enforced upper bound. Add a MAX_LOG_SEGMENTS constant to the rex package and enforce cleanup of segments beyond the limit during every rotation event to prevent unbounded .rex/ directory growth in long-running projects.
- workflow.md has no enforced regeneration trigger — it is the only human-readable file in a zone of machine-written artifacts, and its staleness is silently indistinguishable. Either (a) regenerate workflow.md on every rex write operation or (b) delete workflow.md and link users to 'rex status' for the human-readable view, eliminating the dual-source risk entirely.

</insights>
