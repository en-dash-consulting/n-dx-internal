# Web Static Assets — Zone Context

<!-- Per-zone context generated by Sourcevision. -->

<zone>

Zone: Web Static Assets (`web-static-assets`)
Files: 5, Cohesion: 0.00, Coupling: 0.00
Risk: at-risk (score: 0.50)
Description: A residual zone grouping web viewer static assets (HTML entry point, logo images) alongside root-level cross-package integration tests that have no import-graph connectivity.
Lines: 1495

</zone>

<files>

packages/web/src/viewer/darkmode_logo.png (Other, 0 lines, asset)
packages/web/src/viewer/index.html (HTML, 21 lines, other)
packages/web/src/viewer/lightmode_logo.png (Other, 0 lines, asset)
tests/integration/cross-package-contracts.test.js (JavaScript, 1074 lines, test)
tests/integration/scheduler-startup.test.js (JavaScript, 400 lines, test)

</files>

<findings>

[observation] [info] Isolated files — no import edges between 5 files, cohesion is unmeasurable (reported as 0)
[observation] [warning] Cohesion 0 and coupling 0 confirm no import relationships exist between any files in this zone; the grouping is a Louvain artifact, not an architectural signal.
[observation] [info] Pinning logo PNGs and index.html to web-dashboard and moving tests/integration/ files to the e2e or project-root zone would fully dissolve this residual artifact.
[observation] [warning] This zone spans two distinct package contexts (packages/web/ static assets and tests/integration/ root tests) — a cross-package residual zone that violates the isolation contract documented in zone hints.
[pattern] [warning] Cross-package residual zone in violation of the documented zone prohibition. PNG logo assets and index.html belong to web-dashboard (viewer static files); tests/integration/ files belong to the project-root or e2e zone. This is the highest-priority zone pin action because cross-package groupings produce unreliable coupling and cohesion metrics that could mask real architectural issues.
[anti-pattern] [warning] Root-level integration tests (tests/integration/cross-package-contracts.test.js, tests/integration/scheduler-startup.test.js) are grouped with web viewer PNG assets under a single zone. If a cross-package contract test fails, the zone-level failure metric will surface in web static-asset health dashboards rather than the project-root zone, making the failure harder to route to the correct owner.
[suggestion] [info] Zone "Web Static Assets" (web-static-assets) has at-risk risk (score: 0.50, cohesion: 0.00, coupling: 0.00) — approaching architectural risk thresholds

</findings>

<insights>

- Isolated files — no import edges between 5 files, cohesion is unmeasurable (reported as 0)
- Zero cohesion and zero coupling across a cross-package mix (web assets + root integration tests) mark this as a problematic residual zone; per project conventions cross-package residual zones produce unreliable health metrics.
- The PNG logo assets and index.html should be pinned to web-dashboard (viewer static files), while tests/integration/ tests belong in the project-root or e2e zone.
- Dissolving this zone via pins would eliminate the false impression that web static assets are architecturally related to root-level integration test infrastructure.
- This zone spans two distinct package contexts (packages/web/ static assets and tests/integration/ root tests) — a cross-package residual zone that violates the isolation contract documented in zone hints.
- Cohesion 0 and coupling 0 confirm no import relationships exist between any files in this zone; the grouping is a Louvain artifact, not an architectural signal.
- Pinning logo PNGs and index.html to web-dashboard and moving tests/integration/ files to the e2e or project-root zone would fully dissolve this residual artifact.
- Spans two package boundaries (packages/web/ static assets and tests/integration/ root tests) — the most structurally invalid grouping in this batch, as cross-package residual zones are explicitly prohibited by project zone hints.
- Cross-package residual zone in violation of the documented zone prohibition. PNG logo assets and index.html belong to web-dashboard (viewer static files); tests/integration/ files belong to the project-root or e2e zone. This is the highest-priority zone pin action because cross-package groupings produce unreliable coupling and cohesion metrics that could mask real architectural issues.
- tests/integration/cross-package-contracts.test.js and tests/integration/scheduler-startup.test.js validate cross-package integration contracts at the monorepo level; their misclassification under a web static-assets zone means monorepo-level contract regressions will be attributed to web zone health rather than project-root health in automated reports
- Root-level integration tests (tests/integration/cross-package-contracts.test.js, tests/integration/scheduler-startup.test.js) are grouped with web viewer PNG assets under a single zone. If a cross-package contract test fails, the zone-level failure metric will surface in web static-asset health dashboards rather than the project-root zone, making the failure harder to route to the correct owner.
- [call graph] 25 internal calls, 0 outgoing, 0 incoming (cohesion: 1, coupling: 0)

</insights>
